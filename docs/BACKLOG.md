# ZotHub — Backlog

**The single log** of everything to implement, fix, remove, or decide. If another
document disagrees with this file, **this file wins**.

- **Last updated:** 2026-08-23 — maintainer decisions recorded; see [Decisions made](#decisions-made).
- **Replaces:** `docs/LAUNCH-BACKLOG.md`, plus the scattered "still open" sections in
  `00-handoff.md` §6, `implementation-audit.md`, `plan.md` (WS10–12), the design and
  strategy docs, and the untracked `[launch-blocking]` tags in `prd.md`. See
  [Where the old docs went](#where-the-old-docs-went).
- **Status:** ⬜ open · 🔄 in progress · ⏸️ deferred by decision · ✅ done
- **Impact** is the column that decides *do / defer / skip*:
  **UX** users feel it · **Security** exploitable · **Trust** credibility, privacy, legal ·
  **Reliability** silent failure later · **Housekeeping** nobody notices

---

## Now / next

1. ✅ **Phase 0 — verify the live site. COMPLETE.** All public-side checks, all six
   `verify_prod_state.sql` queries, and the `S2` admin audit are done. Results in the
   findings table below. **No security abuse; no wrongly-hidden clubs; `MB1` and `S2`
   closed.** One action fell out of it: `D1` is wider than thought (3 test clubs).
2. ✅ **Phase 1 — tracking consolidated.** This file is now the only tracker; superseded
   planning docs are archived; stale docs corrected.
3. 🔄 **Phase 2 — UI/UX + pre-launch fix-up. IN PROGRESS.** Defects are logged in
   [Phase 2 — UI/UX defect log](#phase-2--uiux-defect-log) (`UX1`–`UX17`). Read
   [`HANDOFF.md`](./HANDOFF.md) before starting. **Scope decision 2026-08-23: everything —
   no date pressure, the app is finished before launch strategy is planned.**
4. **Order is fixed by dependency, not by size:** `UX15` (adopt TanStack Query) lands
   **first**, because every per-page `UX*` fix touches the same data-fetching code and would
   otherwise be written twice. Then the nav restructure (`UX2`+`UX6`), then the shared
   toolbar (`UX11`/`UX13`), then per-page polish.
5. Everything else waits and is logged below. Nothing has been dropped.

---

## Phase 0 findings — live production state (2026-08-11)

Read-only, from the anonymous (public) side — i.e. **exactly what a visitor sees**.

| Finding | Detail |
|---|---|
| ✅ **Seed healthy** | 725 clubs publicly visible: 724 with a `source` + 1 organic. ⚠️ **Two of the 724 are test rows** (the "Crewmate" clubs below), so the real ZotSpot population is likely **722**. |
| 🔴 **Every club shows grey initials** | **725 of 725** have `logo_url IS NULL` — no club in the directory has a logo. Of those, 589 can be re-hosted and 136 cannot. Most visible quality problem on the site. → `MB5-logo` |
| 🔴 **Test Club is live and public** | "Test Club" owns 5 junk opportunities — `opp 1`, `opp 2`, `opp 3`, `opp 3`, `opp 5` (two share the title `opp 3`, which is what the old "duplicate applications" report actually was). These are the **only** opportunities on the entire site. → `D1` |
| ⚪ **No events visible** | Zero active/public events, though Test Club holds **6** event rows (inactive or past) that the purge must account for. |
| ⚪ **No claims yet** | Zero clubs have `claimed_at` set. |
| ✅ **Backfill was a no-op — nothing wrongly hidden** | Q1 returned **0 rows**; Q2 confirms `hidden_organic = 0`, `hidden_seeded = 0`, `published_total = all_clubs = 725`. No club was hidden by migration `…000400`. **Concern closed.** |
| ✅ **MB1 constraint survived** | Q3: `applications_opportunity_id_student_id_key UNIQUE (opportunity_id, student_id)` **and** `rsvps_event_id_student_id_key` both exist in prod. Duplicate applications are impossible. **`MB1` closed** — see the correction below. |
| 🔴 **Three test clubs live, not one** | Q5: **Test Club** (5 opportunities, **6 events**, 5 applications, 5 RSVPs attached) plus **"Test: Purple Crewmate"** and **"Test: Blue Crewmate"** (both empty). The two Crewmates are *not* counted as organic (Q2 shows only 1 published organic), so they sit **inside the 724 "seeded" figure** — i.e. the real ZotSpot count is likely **722**. → widens `D1` |
| ✅ **Cron inventory** | Q4: `send-reminders-hourly` (`0 * * * *`, active) and `archive-past-events-nightly` (`0 9 * * *`, active). Both live. → unblocks `R2`; also surfaced `S6` |
| ✅ **Logo split confirmed** | Q6: 725 clubs · **0** hosted logos · **589** re-hostable from `source_logo_url` · **136** with no source logo at all. → `MB5-logo` |
| ✅ **S2 admin audit clean** | Exactly **one** admin row: `zothub.uci@gmail.com` (`df22c590-…`), flagged `is_expected_admin = true`, holds no other role, granted `2026-07-09 04:05:56` — ~5 min after the account was created at `04:00:30`, consistent with founder setup. **No self-granted or rogue admins.** The privilege-escalation window was never exploited. |

> **Note on method:** a read-only remote schema dump was attempted and authenticated, but
> the CLI reported "Initialising login role…", which may create a role on the remote. Since
> access this round is strictly read-only, that path was abandoned in favour of SQL the
> maintainer runs. Nothing was written to production.

---

## Open items

### Audit findings — 2026-08-23 (untracked until now)

A 7-way code audit of every claim in this file. **These items were in no document.** Verdicts
were cross-checked between auditors and the contested ones re-verified by hand; `file:line`
references are to HEAD `f82866f`.

| ID | Impact | Item |
|---|---|---|
| 🔄 **A1** | **Security · Trust · UX** | **CODE COMPLETE 2026-08-23, awaiting backend deploy.** New edge function `supabase/functions/provision-oauth-user/index.ts` provisions the account server-side with the service role (mirroring `verify-otp`), derives user id + email from the caller's JWT only, auto-approves students on the same `@uci.edu` proof, keeps clubs queued with `published=false`, and is idempotent (`waitlist` has no unique constraint on `user_id`, so the guard is load-bearing). `AuthContext.handleNewOAuthUser` now calls it and **checks the result**. Declared `verify_jwt = true` in `config.toml`. RLS policies deliberately left untouched. **Deploy order: `supabase functions deploy provision-oauth-user` MUST run before the frontend is pushed.** **Event side (wave 4c):** `eventKeys.detail(id, authScope(user?.id))`, same reasoning. Proved with the full journey — browse an event logged out, sign in through the real login form, walk back to it by clicking Clubs → the club → its event (all client-side), open the RSVP form, answer and submit (`tests/browser/wave4c-event-rsvp.mjs`, 9/9). **The negative run is the clearest evidence in this migration:** with both protections removed, the RSVP form opens with no question at all and the write goes through as `answers: []` — a blank RSVP recorded as a confirmed attendance, which is precisely the reported bug. Original diagnosis follows. | 🔴 **"Sign in with UCI Google" is broken and fails silently — the worst defect on the site.** `AuthContext.tsx:144-163` inserts the new user's `student_profiles` / `club_profiles` row, but the INSERT policies require `has_role(auth.uid(),'student'|'club')` (`20251223013805:73`, `:108`) and the same function deliberately leaves the user role-less (`:175` "Don't set role since they're on waitlist"). **The insert is rejected by RLS and the result is never destructured, so nothing surfaces.** Net: a Google signup gets a `waitlist` row and *no profile*. Compounding: it hard-codes `status: "pending"` (`:135`), so a **student** who signs up with Google is queued for a manual approval that OTP students have not needed since `S3`. One of the two visible signup paths strands the user. **Verified by hand, not just by audit.** **DECIDED 2026-08-23: fix properly now** — server-side profile creation + student auto-approval + check the result. Do not loosen the RLS policies; they are correct. **Fix written, then adversarially tested against a local stack, which found 4 real defects IN THE FIX — all now corrected:** (a) 🔴 **the idempotency guard was not concurrency-safe and failed the wrong way.** Five concurrent first-calls all passed the guard; one won the `user_roles` insert and the rest hit `user_roles_user_id_role_key`, which the code treated as "role grant failed → send to the queue". Two observed consequences: approved students told `autoApproved:false` and routed to `/waitlist`, and a reachable **contradictory state — role granted while `waitlist.status='pending'`**, parking a phantom student in the admin queue. Fixed: `23505` is now success, and `autoApproved` derives from the role's actual presence (with a confirming read before any demotion), never from which insert won. (b) **a comment's stated rationale was false** — it claimed `waitlist` has no unique constraint on `user_id`; line 4 of `20260128214112` is `user_id UUID NOT NULL UNIQUE`. The DB constraints are the integrity mechanism; the guard is a racy fast path. Corrected. (c) 🔴 **the domain check was STRICTER than the gate it claimed to mirror.** It cited `20260709000300`, which `20260727000200` superseded: the live trigger admits `@uci.edu` **or** the admin allowlist **or** a live `signup_email_authorizations` grant — the supported path by which non-UCI **club** accounts are legitimately created. Demonstrated: an authorized `officer@someclub.org` account was **403'd** by the function. Fixed to mirror all three cases, with ILIKE wildcards escaped (`%`/`_` are legal in an email local part, so an unescaped value could match a different address's row). (d) **the send-email result was discarded**, violating `CLAUDE.md`'s mandatory shared checker; now routed through `checkEmailResult` and reported as `emailDelivered`. **Confirmed still sound:** identity cannot be spoofed (body-supplied `userId`/`email`/`sub` ignored — only the JWT's user provisioned), unauthenticated rejected 401 at both the gateway and the function's own guard (**including service-role-key-as-bearer**), bad roles 400 with nothing written, and the club path correctly queued + `published:false` + absent from `get_all_clubs_public`. |
| ✅ **S7** | **Security** | **The service-role path is intact — the high-stakes check.** `send-reminders` can still write `reminder_logs` and `unique_reminder` still dedupes for it, so it will **not** start re-sending hourly duplicates. **APPLIED TO PRODUCTION 2026-09-19** (`20260824000100_close_open_write_policies.sql`) — `supabase db push --linked` reported "Applying migration … Finished". Previously verified by four clean applies against a throwaway local stack. Applied **four times** against a throwaway local stack with zero errors and **no policy accumulation** (one INSERT policy per table, `roles = {service_role}`, `non_service = 0`). The permissive INSERT policy is replaced with one scoped `TO service_role`, **and** the table-level INSERT privilege is revoked from `anon`/`authenticated` — because RLS and grants are each individually sufficient to reopen the hole, so both are closed. Note `20260727000500` granted service_role but never revoked anon/authenticated, which is why the policy was reachable. Original diagnosis follows. | 🔴 **Any anon-key holder can permanently mute any user's reminder emails.** `reminder_logs` INSERT is `WITH CHECK (true)` with no `TO` clause and was never revoked (`20260121001924:37-40`). Combined with the `unique_reminder` constraint (`:30`) and the pre-send dedup, forging a log row makes that reminder **unsendable forever**. Weaponises `R1`. **Note the cron stays ON by decision 2026-08-23**, so `S7` and `R1` are the pair that must land before the first real user is onboarded. |
| ✅ **S8** | **Security · Trust** | **VERIFIED 2026-08-23, cleared to apply**, same file. **The hole was proven real, not theoretical:** on the same database, the migration's policy refused a forged insert while restoring the original permissive policy let it through (`INSERT 0 1` — a forged row created), reproduced both via `SET ROLE` and through PostgREST. **The new trigger was proven correct too**, including the reasoning behind BEFORE DELETE: the cascade takes the RSVP rows from 4 to 0, so an AFTER DELETE trigger would have found an empty attendee list. Exactly the confirmed attendees are notified; pending and cancelled RSVPs get nothing; a zero-RSVP event deletes cleanly. **And nothing regressed** — the existing `notify_club_on_new_application` and `notify_rsvp_status_change` triggers still fire, empirically confirming that table-owner `SECURITY DEFINER` functions bypass both the policy and the REVOKE. **Approach decided by the maintainer: move the capability into the database.** The only legitimate client-side notification INSERT was the event-cancellation loop in `eventNotifications.ts`; that is now a `notify_attendees_on_event_delete` **BEFORE DELETE** trigger on `events` — before, because cancelling an event is a hard `DELETE` (`useClubEvents.deleteEvent`) and `rsvps.event_id` is `ON DELETE CASCADE` (`20251223013805:252`), so by AFTER DELETE the attendee list is gone. Authorisation is inherited rather than re-implemented: the `events` DELETE policy already restricts deletion to the owning club. The browser's INSERT policy and privilege are then both removed. Deliberately **not** gated on a notification preference — every other notification here is, but "the event you signed up for is cancelled" is not promotional, and this also preserves the previous behaviour exactly. Frontend cleanup done in the same change: the browser loop is gone, and `sendEventCancellationEmails` now takes only `eventId`, which let **two now-pointless queries** be deleted from `deleteEvent`. Original diagnosis follows. | 🔴 **Any authenticated user can forge an in-app notification for anyone**, with arbitrary title and body — `notifications` INSERT is the same open shape (`20251223160240:176-178`). Load-bearing by design: the browser inserts directly (`eventNotifications.ts:39-45`) and a migration depends on it (`20260710000100:12-13`), so the fix needs an RPC/trigger **before** the policy narrows. |
| ✅ **S9** | **Security** | **MIGRATION WRITTEN 2026-08-23**, same file. Limits chosen by the maintainer: **images + PDF, 10 MB each** on `club-assets`; `student-resumes` gets the same ceiling with a document mime list. ⚠️ **Correction to the original diagnosis:** the INSERT policy is *not* free-for-all — `auth.uid()::text = (storage.foldername(name))[1]` does confine each user to their own uid-named folder, so this was never cross-user tampering. The real exposure was **unlimited file type and size in a publicly-readable bucket**, since the client-side `accept`/`maxSizeMB` guards run in the browser and are trivially bypassed. Bucket limits are enforced for every caller including service role — verified as safe for the `MB5-logo` bulk re-host, which is far inside them. Original diagnosis follows. | 🔴 **The `club-assets` bucket is free public file hosting for anyone with an `@uci.edu` account.** Created `public=true` with no mime allowlist, no size limit, and an INSERT policy with no role check (`20251223165608:2-15`), plus unconditional public SELECT (`:31-33`). The client-side `accept` / `maxSizeMB` guards are trivially bypassed. Runs on the project's quota. |
| ⬜ **S10** | **Reliability · UX** | 🔴 **A green build is not a shippable build.** `npm run build` **succeeds with `VITE_TURNSTILE_SITE_KEY` absent** — and `.env.example` never mentions the variable. Per `CLAUDE.md` that artifact renders a visible error and **blocks signup and club claims**. So the documented failure mode is not just possible, it is the *default* for a fresh deploy, and nothing catches it. Fix = assert the var in the build and add it to `.env.example`. **✅ DONE 2026-08-23.** `.env.example` documents `VITE_TURNSTILE_SITE_KEY` with the fail-closed consequence spelled out, **and `vite.config.ts` now aborts a production build that is missing it** (or the two `VITE_SUPABASE_*` vars), with an error naming exactly which are absent and pointing at Cloudflare's public test key for local use. Deliberately **no bypass flag** — a flag that skips this check is a flag someone eventually ships. Verified by running: no key → **exit 1**; key present → exit 0; env-vars-only with no `.env` file (the CI case) → exit 0, confirming `loadEnv` reads `process.env`. The CI workflow additionally asserts the *failure*, so a regression that makes the build quietly succeed is itself caught. |
| ✅ **T1** | **Reliability** | **DONE 2026-08-23.** Two layers, because they do different jobs. **(1) The deploy gate:** `vercel.json` now sets `buildCommand` to `npm run typecheck && npm run lint && npm run build`, so a failure fails the *build* and Vercel publishes nothing — the previous deployment stays live. This is what actually stops a bad deploy; a separate CI job cannot, because Vercel starts building the instant the push lands and would win the race. **(2) Visibility:** `.github/workflows/checks.yml` runs typecheck + lint + the 14 unit tests + a production build on every push and PR, giving a queryable pass/fail mark. ⚠️ **Deliberate asymmetry, documented in the workflow:** the unit tests are NOT in the deploy path, because they need Node 22's `--experimental-strip-types` and Vercel's Node version is set in its dashboard — had they been included and Vercel been on Node 20, the gate would have failed *every* deploy. So a unit-test failure shows red but does not block; typecheck/lint/build failures do block. Scope per maintainer decision: fast checks only (~2 min); the 115-assertion security suite stays a deliberate manual step. Original text follows. 🔴 **There is no CI at all** — no `.github/`, no hooks — while Vercel auto-deploys every push to `main`. Nothing prevents shipping a red build to production. `playwright.config.ts:17-18` and `tests/e2e/run.sh:27` already branch on `process.env.CI`, so the suite was written expecting a CI that was never built. |
| ⬜ **T2** | **Reliability · Trust** | 🔴 **The E2E suite reports `ALL GREEN` while skipping 24 of its 115 assertions**, and exits 0 — so "115/115" in `CLAUDE.md` and `HANDOFF.md:145` is unverified. Without a Docker daemon it runs 91; the skipped set includes transactional approval rollback and fail-closed rate limiting. `run.sh:18` hardcodes a macOS Docker path, so it has demonstrably never run elsewhere. **MEASURED 2026-08-23.** A Docker daemon CAN be started in the cloud session (`sudo dockerd`), so the suite was run for real. **With Docker: `PASSED 112, FAILED 3, EXECUTED 115/115`, zero skips** — so the 115 total is correct and all three `sqlReady()` blocks do run. **Without Docker: 91 execute and 24 never run** (approval-rollback 10, rate-limit-fails-closed 5, pending-club backfill 9), confirming the earlier estimate exactly. The 3 failures are **environmental, not code defects**: the sandbox's edge-runtime container cannot complete TLS to `api.resend.com`, so the SDK throws and `send-email` returns 500 where the test expects the `HTTP 200 + {error}` false-success shape. They fail identically with and without Docker. **HARNESS BUG FIXED:** `skip()` counted *blocks* not assertions and `ALL GREEN ✅` printed whenever `failures.length === 0`, so a run that silently dropped 24 assertions read as `PASSED 91, FAILED 0, SKIPPED 3 / ALL GREEN` — indistinguishable from a pass, and the origin of the docs' "115/115" claim. The suite now declares `EXPECTED_ASSERTIONS = 115`, prints `EXECUTED n/115`, and **exits 1 if any assertion did not run**. Still open: the run.sh macOS Docker path, and the production-write footgun in the standalone README procedure. **Also a production footgun:** no hostname guard, and `claim-and-signup.e2e.mjs:468` renames `rate_limit_hit` — a maintainer following `tests/e2e/README.md:27-31` with prod keys exported would write heavily to production and leave that rename in place if the run died mid-way. |
| ⬜ **UX18** | **Trust · UX** | 🔴 **"Reject All" and single accept/reject fire instantly on named students with no confirmation and no undo** — zero `AlertDialog` / `confirm(` anywhere in `ApplicationReview.tsx` — and the rejection email is already sent. One misclick permanently rejects a queue of real people. Logged in `prd.md:173` and rated severity 4 in research; **the tracker dropped it.** |
| ✅ **D3** | **Trust** | **DONE 2026-08-23** — the false data-export sentence is deleted from `Privacy.tsx`, and the deletion bullet now names the manual email process honestly. Verified in the browser, both themes, 1280/375 widths, 0 console errors. Original text follows. | 🔴 **The live privacy policy promises a data export that does not exist.** `Privacy.tsx:114`: "you can view and export your personal data from your profile settings". There is no export anywhere in the app. With `MB6` deferred, `Privacy.tsx:116` also routes deletion requests to the unreachable address in `UX4`. **DECIDED 2026-08-23: delete the sentence outright** — no reword, no export build. |
| ✅ **UX19** | **UX** | **A signed-in club has no way to reach any discovery page.** `CLUB_NAV` (`navConfig.ts:63-101`) contains no `/clubs`, `/opportunities` or `/events` destination — clubs cannot see the directory they are listed in. Also `navConfig.ts:59,86` match a `/messages` prefix that **has no route**. Fold into the `UX2`+`UX6` nav work. |
| ⬜ **E1** | **Reliability** | **Three more email paths bypass the shared delivery checker** — `CLAUDE.md` says `send-reminders` is "the one". It is the only path bypassing `send-email`, but `checkEmailResult` is also skipped by `verify-otp/index.ts:271-287` (never reads the response), `eventNotifications.ts:70-82` (returns `success: true` on a 200 carrying `{error}` — while the *same file* uses the checker correctly at `:25`; callers `RSVPReview.tsx:231,290`), and `AuthContext.tsx:168-173` (discards the result). Correct the `CLAUDE.md` wording too. |
| ⬜ **H3** | **Security · Housekeeping** | **17 npm vulnerabilities (6 moderate, 11 high)** reported by `npm ci`, never audited or tracked. |
| ⬜ **DP10** | **Housekeeping · Reliability** | **Migration `20251223165608` is not idempotent** — bare `INSERT INTO storage.buckets` and nine bare `CREATE POLICY` — so `README.md:85`'s "run the migrations" instruction **fails** on any project that already has the buckets. |
| ⬜ **D4** | **Reliability · Trust** | **`README.md` advertises features that do not exist.** `:41` and `:155-159` describe "efficient server state caching with TanStack Query", "optimistic UI updates" and "automatic data refetching and invalidation" — ⚠️ **Partly overtaken by events, 2026-09-19:** `UX15` waves 0-3 have shipped, so caching and invalidation are now real on the identity slice, the shared hooks and the three list pages — the README is no longer *false*, but it still describes the finished state rather than the actual one, and "optimistic UI updates" remains unimplemented (see `T9`). Rewrite it when wave 4 lands, not before. Original: there were **zero** `useQuery`/`useMutation` calls in `src/`, which was the single most misleading passage for a new contributor, and it was **not** part of `D2`. Also `prd.md:136` still says "Four Edge Functions" (there are six — `f82866f` fixed the README and missed the identical error in the PRD), `README.md:176`/`:20` and `prd.md:230` list the dead `club_followers` table (`H1`), and `prd.md:306-309` says "the **four** launch-blocking items" then names five with six in the catalog. |
| ✅ **UX20** | **UX** | **DONE 2026-08-23 — a dark-mode device was served the light theme on first visit.** `App.tsx` had `enableSystem` set but `defaultTheme="light"`, and next-themes only consults the OS when `defaultTheme` is `"system"` — so the default silently overrode every visitor's device preference. Found by testing in the browser, not by reading. Now `defaultTheme="system"`. Verified all four cases: device-dark→dark, device-light→light, no-preference→light, and the manual `ThemeToggle` still overrides both. |
| ⬜ **DP11** | **Housekeeping** | **Stale comment in an applied migration.** `20260710000100`'s header says its trigger inserts "via the existing 'System can insert notifications' policy". That policy is removed by `20260824000100`, and the trigger works regardless — table-owner `SECURITY DEFINER` functions bypass both the policy and the REVOKE (proven empirically, not assumed). **Deliberately NOT fixed by editing that file:** it is already applied in production, so editing it would make the checked-in migration diverge from what the database actually ran, and anything rebuilding from scratch would execute a different file than production did — exactly the drift that has already cost this project time. This backlog entry *is* the fix: it makes the stale comment findable without rewriting applied history. **Do not re-add a permissive policy on that comment's account.** |
| ✅ **DP12** | **Housekeeping** | **DONE 2026-09-21 — both files deleted** (maintainer decision the same day). Typecheck and production build both clean afterwards, which is the proof nothing imported them. Original text follows. | **Dead code carrying broken links.** `src/components/dashboard/DashboardLayout.tsx` is imported by nothing (verified 2026-09-21, whole-tree grep), and **five of its six sidebar links point at `/applicants?tab=…`** — a query-param scheme `ClubHome` stopped honouring when it moved to path-based sections. `src/pages/Index.tsx` is also unrouted boilerplate ("Welcome to Your Blank App"). Found during the `UX12` CTA sweep. Not deleted in that commit because removing files is a different change from correcting link targets, and the sweep's test covers the live CTAs, not these. **The risk is a future session copying the stale `?tab=` pattern from a file that looks authoritative.** Deleting both is the fix; nothing imports either. |
| ✅ **A5** | **Access** | **FIXED 2026-09-21, after being reproduced in a browser rather than inferred.** `ProtectedRoute` now honours KNOWN states first (waitlist pending/rejected win even if the role read failed — a pending user has no role by design), never guesses an UNKNOWN in either direction (`roleError` / `waitlistError` render `VerificationFailed` **in place**, so a successful retry returns to the page they asked for), and only then decides entitlement (`allowedRoles && !role` → the new `/account-setup`). `useWaitlist` and `AuthContext` both gained an error flag, because collapsing "the read failed" into "no row" was the fail-open. **`tests/browser/a5-role-guard.mjs`, 16 checks**, and restoring the original fall-through makes 6 of them fail with the real symptom ("landed on /applicants"). **Half those checks exist to catch the OPPOSITE mistake** — an approved student or club must still get in, cross-role redirects must still work, and a rejected account must still reach its page; failing closed would have traded a silent let-in for a silent lock-out. Original diagnosis follows. | ⚠️ **REPRODUCED IN A BROWSER 2026-09-21 — real, and wider than first logged.** A signed-in account with **no role** renders **every** protected page, on **both sides**: `/activity`, `/profile`, `/messages` AND the club-side `/applicants`, complete with the club nav. Two separate triggers, both reproduced: (a) **no waitlist row** — provisioning never finished, e.g. a Google sign-in with no intended role stored; (b) **the waitlist read simply FAILING** — `useWaitlist` catches the error, logs it, and sets `status = null`, which the code comments define as "approved (or admin)". So a transient network failure on one query makes a pending user look approved. The intended path is fine: with the row present and `pending`, all four routes correctly redirect to `/waitlist`. **Root cause:** `ProtectedRoute`'s last check is `allowedRoles && role && !allowedRoles.includes(role)` — when `role` is `null` the condition is false, so it falls through to `return <>{children}</>`. `AdminRoute` handles the same case explicitly, so the two guards disagree. **NOT a data-exposure hole, and that matters for priority:** RLS is the real gate — creating a student or club profile requires `has_role` (`20251223013805:73,:108`), so a role-less account cannot write, and the pages render empty. The defect is a **fail-open guard**: a confusing broken experience today, and a genuine hole the day a page is added whose protection assumes the guard works. **The fix must distinguish "no role" from "could not read the role"** — `fetchUserRole` also sets `role = null` on error, so failing closed naively would lock a legitimately approved user out on a network blip, trading a silent let-in for a silent lock-out. Same principle as `O6`/`UX17c`. **Safe to gate on the role:** approval UPDATES the waitlist row to `approved` *and* grants the role, so every usable account has one — gating on "no waitlist row" would NOT be safe, and is the wrong axis. | **Original:** A signed-in user with no role and no waitlist row passes every guard.** `ProtectedRoute` checks, in order: not signed in → `/login`; waitlist pending/rejected → those pages; `allowedRoles && role && !allowedRoles.includes(role)` → bounce. When `role` is `null` that last condition is false, so the user **renders the children** — e.g. a role-less account reaches the student Activity page. `AdminRoute` handles the same case explicitly (`/`), so the two guards disagree. Found during the `UX12` CTA sweep by reading the guards, **not reproduced against a real account** — how an account reaches this state at all is the first thing to establish. Related: `Login.tsx` has no terminal branch for it either, so such a user sits on `/login` with no message. |
| ✅ **UX24** | **UX · Trust** | **FIXED 2026-09-21, reproduced in a browser first** (`tests/browser/ux24-follow-notification.mjs` — the failing run printed "rendered as plain text — nothing to click"). `getNotificationLink` gained a `new_post` case that reads the generated title to pick the surface, so both a new role and a new event open the thing they are announcing, and mark-as-read fires again because it hangs off that link. Frontend-only by maintainer decision: it needs no deploy step and it fixes the notifications **already sent**, which a new notification type would not. ⚠️ **It depends on a string the migration generates** (`'New opportunity from …'` / `'New event from …'`) — change that title and this link goes quiet with nothing failing loudly; a comment pins the dependency at both ends. An unrecognised title returns no link rather than guessing, because guessing wrong lands the student on a detail page for an id that does not exist there. Original diagnosis follows. | **A follow notification is a dead end — the one thing "Following" promises does not work when you tap it.** Found 2026-09-21 while checking the empty-state copy before writing it. The trigger `notify_followers_on_new_post()` inserts `type = 'new_post'` with `related_id` = the new post's id (`20260710000300:46-53`). `NotificationCard.getNotificationLink()` has cases for `application_update`, `new_application`, `new_message` and `event_reminder` and **returns `null` for everything else** (`:26`), so a follow notification renders as **plain text, not a link** — a student is told "Hack at UCI just posted: Marketing Lead" and cannot get to it. **Second effect:** `onMarkAsRead()` is wired to the `<Link>`'s `onClick`, so it never fires for these either — the unread count stays lit after reading. Breaks Foundation rule 1 (never a dead end). **The awkward part:** `'new_post'` does not say WHICH table `related_id` points at; only the generated title does (`'New opportunity from …'` / `'New event from …'`). So the fix is either a frontend match on that title prefix (works today, depends on a string our own trigger generates) or a migration splitting the type into `new_opportunity` / `new_event` (clean forever, needs the maintainer to apply it, and leaves existing rows on the old value). **Not yet reproduced against a real notification row** — read from the trigger and the resolver. |
| ✅ **UX25** | **Trust · Compliance** | **FIXED 2026-09-21. The unsubscribe link in a "new post from a club you follow" email switched off the WRONG preference.** `send-email`'s `new_club_post` template called `getEmailFooter("deadline_reminders")`, so the link read "Unsubscribe from deadline reminders" and sent `?type=deadline_reminders` — a student trying to stop new-post emails kept receiving them and silently lost their deadline reminders instead. `Unsubscribe.tsx` already supports `new_post_notifications`, so this was a one-word fix. **Found while routing `send-reminders` through `send-email` for `S5`, and it matters beyond itself: the inline HTML being replaced had the link RIGHT, so applying the `S5` fix naively would have regressed a working opt-out into a broken one.** A comment now pins why that footer argument is what it is. |
| ⬜ **A6** | **Access** | *(Still open — `A5`'s fix did not touch Signup's redirect effect.)*  **`Signup.tsx` has no waitlist branch where `Login.tsx` does.** Login sends a signed-in, role-less user to `/waitlist` or `/waitlist-rejected` depending on their row; Signup's redirect effect only handles `user && role`, so a pending club sitting on `/signup` is not redirected at all. Cosmetic today (they would have to navigate there deliberately), but the two pages implementing the same rule differently is how the next inconsistency gets introduced. Found during the `UX12` CTA sweep. |
| ✅ **T3** | **Reliability** | **DONE 2026-08-23 — `eslint .` linted `supabase/.temp/`, producing 207 phantom errors.** That directory is written by `supabase start` and contains a bundled, minified edge-runtime entrypoint. It is gitignored, but **ESLint flat config does not read `.gitignore`**, so any machine that had ever run the local Supabase stack got 207 errors from `npm run lint`. Found while testing the new build gate — it would have failed every deploy from such a machine for reasons unrelated to the code. `eslint.config.js` now ignores `supabase/.temp`, `supabase/.branches`, `playwright-report` and `test-results` alongside `dist`. |
| ⬜ **A2** | **Security · Trust** | 🔴 **Nothing cleared the query cache on sign-out — and this defect was INTRODUCED by the UX15 work itself.** `clearProfileCache` shipped in `useProfileLookup.ts` with **zero callers**, and `AuthContext.signOut` cleared only the role and localStorage. Harmless while the app had no cache (every mount refetched), but with `gcTime: 5m` the previous account's rows — profile, bookmarks, applications, unread counts — stayed resident in the same tab after signing out. Per-user query keys stop account B *reading* account A's entry; they do not evict it. **FIXED 2026-09-19:** `signOut` now calls `queryClient.clear()`, and the dead `clearProfileCache` is deleted so there is only one way to do this. `clear()` rather than per-key removal deliberately — a key added later and not listed would silently survive, and forgetting one is a privacy leak rather than a visible bug. **This is the precondition for every user-scoped key the migration adds.** |
| ✅ **UX21** | **Trust · UX** | **FIXED 2026-09-19 — both sides.** `opportunityKeys.detail(id, authScope(user?.id))` now carries the viewer **id**. Proved in a browser end to end — browse a role logged out, sign in through the real login form, click back to the same role, and the application form carries the club's required question (`tests/browser/wave4b-ux21.mjs`, 8/8). **Proved in reverse, and the result is worth recording precisely:** with the viewer removed from the key but `A4` (clear-the-cache-on-sign-in) still in place, the test **still passes** — `A4` alone covers this particular sequence. With BOTH removed, the form renders "No additional questions" and the bug reproduces exactly as described. So the two mechanisms are redundant *for this path*, and both are kept deliberately: `A4` is a behaviour of `AuthContext` that a future change could drop, while the viewer-scoped key is structural and also covers an account switch inside `gcTime`. Original diagnosis follows. | 🔴 **The migration would CREATE a data-loss bug if the viewer is left out of the detail query keys.** `EventDetail.tsx:74` selects `rsvp_questions` only when authed, and `OpportunityDetail.tsx:89` selects `application_questions` only when authed (anon holds no column grant). Cache a page while logged out, then log in, and the anon-shaped entry is served for up to `gcTime` — `hasQuestions` (`useEventRSVP.ts:137`) reads false and **the student RSVPs with `answers: []` to an event whose club requires answers**; the opportunity side passes `questions={[]}` at `OpportunityDetail.tsx:461` and submits an application with zero answers. Mitigation is mandatory and specified: key by `authScope(user?.id)` — the viewer **id**, not an `"auth"`/`"anon"` boolean, because the embedded `rsvps`/`applications` arrays are RLS-shaped per viewer, so an `"auth"` bucket would serve account A's row visibility to account B. Verified in the browser: the two viewer variants produce distinct keys and both sit under the `details(id)` invalidation prefix. |
| ✅ **A3** | **UX · Trust** | **FIXED 2026-09-19 — a bug the migration would have introduced, caught by the survey before it shipped.** `StudentDashboard.handleUnfollow` (`:254-274`) deletes a club bookmark **directly, bypassing `useBookmarks` entirely**. Invisible before caching, because every page refetched on mount. With `gcTime: 5m` it meant unfollowing a club from the dashboard left that club's own page still reading **"Following"** for up to five minutes. The handler now invalidates `bookmarkKeys.byType(userId,"club")` and `studentActivityKeys`. A full refactor of the dashboard onto the hook belongs to a later wave; this is the minimal correct fix so the two cannot disagree. |
| ✅ **R3** | **Reliability** | **FIXED 2026-09-19.** Channels are now named per user (`nav-counts-messages:${userId}`), and the subscriptions are hoisted out of the page-level layouts into a `<NavigationCountsSync />` mounted **once** in `App.tsx`, inside `AuthProvider` and outside `<Routes>`. **Hoisting was the actual fix — migrating the reads alone would not have touched the churn**, because `StudentLayout`/`ClubLayout` render inside each page, so every route change unmounted them and tore both websocket channels down. The effect also now depends on `userId` (a string) rather than the `user` object, which `AuthContext` replaces on every `TOKEN_REFRESHED`. Verified in a browser: 1 socket after first load, still 1 after 8 client-side navigations, 0 churn. (Signed-out, so the channels themselves are not exercised — the per-user naming and single mount are verified structurally.) The handler **invalidates** rather than marking stale, because with `staleTime: 60s` marking stale does not refetch; and the count is never derived from the realtime payload, since neither table has `REPLICA IDENTITY FULL` so `payload.old` carries only the primary key. Original diagnosis follows. | **Realtime channel names are global constants, so two mounted instances collide.** `useNavigationCounts.ts` uses `"nav-counts-messages"` / `"nav-counts-notifications"`; with `v7_startTransition` two instances can be mounted at once, both join the same Phoenix topic, and the outgoing one's `removeChannel` can leave the survivor with a dead subscription — counts silently stop updating. Must become `nav-counts-messages:${userId}`. Same class, logged separately and OUT of the current scope: `"notifications-realtime"` (`useNotifications.ts:94`) and `"messages-realtime"` (`useMessages.ts:344`). Also: the hook is called from layouts that render *inside* each page, so a route change tears the channels down and rebuilds them on **every navigation** — migrating the reads does not fix that; the subscriptions must be hoisted to a component mounted once in `App.tsx`. |
| ⬜ **T4** | **Reliability** | **`.limit(50)` truncation will feel more fixed than it is.** Caching makes the arbitrary "first 50" window *persist* rather than be re-rolled on each visit, so "Saved" / "Following" / "This month" consistently search the same 50 rows. Pre-existing (`MB3`), but the caching change alters how it presents — do not paper over it. |
| ✅ **UX22** | **UX** | **FIXED 2026-09-19 — the signed-out "Saved" filter was a dead end.** Tapping the Saved chip while logged out rendered *"You haven't saved any events yet. Save one from a card and it'll wait for you here."* — blaming a visitor for not doing something they had no account to do. Now renders `SignInToSaveState` (`src/components/discover/SignInToSaveState.tsx`): *"Saving needs an account — it takes a minute."* with **Create account** and **Log in**. Shipped on both Events and Opportunities. Safe to link at `/signup` because the branch is unreachable for an authenticated viewer (and `/signup` bounces signed-in visitors). Verified in a browser on both pages, signed out and signed in, including that the visitor can still leave the filter. |
| ⬜ **T13** | **UX** | **Time-boundary drift is a real behaviour change, not a bug — recorded so it is not "discovered".** Cached `gte(now)` lists can outlive their boundary by up to 60s of active use (up to `gcTime` = 5 min across a remount): an event that has just started stays listed, a role whose deadline has just passed stays visible. Nobody can **act** on a stale row — the detail page re-checks `deadlinePassed` and the `enforce_rsvp_capacity` trigger is authoritative — so the drift is cosmetic. Maintainer decision 2026-09-19: accepted at 60s. |
| ✅ **A4** | **Trust · Reliability** | **FIXED 2026-09-19 — the cache was cleared on sign-OUT but never on sign-IN.** `AuthContext` cleared on `signOut` only, and the two public list keys deliberately carry no viewer segment (they are shared by every visitor) while their rows embed RLS-filtered `applications` / `rsvps` arrays. So browsing `/opportunities` logged out, signing in, and coming back within `staleTime` served the **anon-shaped** rows to an authenticated viewer with **zero** network requests — every applicant count stuck at 0. Now `AuthContext` clears the cache when the signed-in identity actually **changes**, gated on the user id rather than on the `SIGNED_IN` event (supabase re-emits that event on session restore and, in some versions, on tab focus — clearing on the event itself would throw the cache away repeatedly, which is the exact behaviour this migration exists to remove). Found by an adversarial review of the wave-3 diff. **Proved both ways in a browser:** with the fix, signing in and navigating back by link click refetches and the count corrects itself (0 → 3); with the guard temporarily disabled, the same test fails with **0 new requests** and the count stuck at 0. |
| ✅ **UX23** | **UX** | **FIXED 2026-09-19 — a signed-in student briefly saw "Apply" on roles they had already applied to.** On a cold load the page runs two serialised reads (`student_profiles` → `applications`) before the Applied badge can be decided, and `Opportunities.tsx` deliberately does not gate the whole list on that second read (doing so would make a signed-in student wait longer than a visitor for the same rows). During the gap, an already-applied role renders an enabled **Apply**; tapping it opens the dialog and the submit fails with a `23505` "You have already applied". **Pre-existing — identical before the migration, so not a regression** — but the migration contract asked for it to be closed and it was not, because *what the button should look like while the answer is unknown* is a design decision. Related, same lines: **`appliedQuery.isError` is surfaced nowhere.** If the applications read fails, the badges are silently wrong with no retry affordance (also parity — the old `catch` just logged). **Fixed:** `Opportunities.tsx` now distinguishes "not applied" from "we do not know yet" and passes `isApplicationStatePending` to the card and the list row, which render a **disabled** Apply — same word, same size, so nothing shifts when the answer lands. Only signed-in viewers have an unknown: a visitor has applied to nothing and a club account resolves to no student profile, and an errored read counts as settled rather than leaving a permanently dead button. Verified in a browser by delaying the applications read and checking both states, and proved in reverse — with the guard removed the same control renders as a live `<a>`. `tests/browser/wave3-ux23.mjs`. **Still open, logged here rather than silently accepted:** `appliedQuery.isError` is surfaced nowhere, so a failed applications read leaves the badges quietly wrong with no retry (parity — the old `catch` only logged). |
| ✅ **O5-counts** | **Trust · UX** | **FIXED AND LIVE 2026-09-20.** Migration applied to production by the maintainer, frontend pushed after. **Verified on the live database read-only, as a logged-out visitor:** `applications_count` returns real backfilled values (a control request for a non-existent column returns `42703`, so the 200 is meaningful), and `confirmed_rsvps_count` answers 200 — i.e. the column-level anon grant, the one step this migration could most easily have shipped without, works in production. Trigger-maintained counters (`opportunities.applications_count`, `events.confirmed_rsvps_count`) replace counting RLS-filtered embedded arrays. **The event side was worse than first logged:** the card's "spots left" was computed from an attendee count of 0 for a logged-out visitor, so a **sold-out event advertised every seat as still available**. Also fixed along the way: the "Most applied to" sort read the same broken array, so it was a no-op for visitors — every row was 0 and the order never changed; and `useOpportunityDetail` defaulted `show_application_count` to `true` while the database defaults it to `false`, showing counts on roles whose club never opted in. Proven with `scripts/test_o5_counters.sql` (37/37 against a throwaway database: every status transition, moves between parents, agreement with the capacity trigger, drift repair, and the anon grants) and `tests/browser/o5-counts.mjs` (5/5). **Deploy order: the migration must be applied BEFORE the frontend commit is pushed**, or every list query 400s on a column that does not exist yet. Original diagnosis follows. | **The applicant and attendee counts on the cards are wrong for almost everyone, and always have been.** The SELECT policy returns only the viewer's own `applications` / `rsvps` rows plus the owning club's, so the embedded arrays the cards count are RLS-shaped: a signed-out visitor sees **0 applied** / **0 going** on every card, and a signed-in student sees **0 or 1**. Only the owning club sees the truth. Verified by reading `20251224045225_10d4c255….sql:80-95`, not by querying the database. This is a **trust** item, not a performance one — "12 applied" is social proof, and showing 0 makes a busy campus look dead. `A4` removed the *caching* amplifier (an anon count is no longer served to a signed-in viewer), but the underlying number is still wrong in both states. The fix is a count that does not depend on the viewer — a `SECURITY DEFINER` aggregate or a denormalised counter — which is a **backend migration**, hence out of scope for the frontend waves. Decide before any polish is wired around these numbers. |
| ⬜ **P2** | **UX** | **`ClubAnalytics` is a 422 kB / 114 kB-gzip chunk**, the largest asset by 2.5×. Confirm it is genuinely lazy-loaded before launch traffic. |

---

### Launch blockers

| ID | Impact | Item |
|---|---|---|
| ⏸️ **D1** | UX · Trust | **Purge test data — DEFERRED BY DECISION to the final pre-launch step (2026-08-11).** Test Club is the only club with real data attached, so it is needed to verify `N1`–`N7`. Delete it **last**, immediately before launch. Targets: Test Club `57ea4a11-…` (5 opportunities, 6 events, 5 applications, 5 RSVPs) + `Test: Purple Crewmate` `d689c87c-…` + `Test: Blue Crewmate` `56d45091-…` (both empty, both carry a non-null `source` so they sit inside the 724 seeded count → real total ~722). Runbook ready: **`scripts/purge_test_data.sql`** (review steps, transactional delete with rollback guard, auth cleanup, post-state check). Back up before STEP 3. **See `D1a` for the interim.** |
| ⏸️ **D1a** | Trust | **NOT NEEDED — deferred by decision 2026-08-23.** The maintainer confirmed **nobody has the link**, so there is no public audience to protect from the junk data, and Test Club is needed intact for `N1`–`N7`. Re-open this the moment anyone outside the team gets the URL before `D1` runs. Original plan: hide the junk from the public site without deleting it. Test Club's 5 opportunities (`opp 1/2/3/3/5`) are the **entire** public opportunity inventory of zothub.app today. Setting `is_active = false` on those 5 rows removes them from public discovery immediately (RLS is `USING (is_active = true)`), is a one-line reversible `UPDATE`, and **keeps the club and all its data intact for testing**. Its 6 events are already inactive. Do this if the site is reachable by anyone before launch; skip it if traffic is genuinely zero. |
| ✅ **MB4** | UX · Trust | **DONE 2026-08-23.** `/help` ships: 16 Q&As across students / clubs / accounts+privacy, a contact block, and a "Report a problem" button opening a pre-filled mailto. `/faq`, `/support` and `/contact` redirect to it. **The three dead-end promises now resolve:** `WaitlistRejected.tsx` and `TurnstileWidget.tsx` (the fail-closed path) link to it, and the privacy policy's manual route has a reachable address. Shell mirrors `Privacy.tsx` per Foundation rule 4; accent uses the designed `--accent-wash`/`--accent-text` tokens. **Verified in the browser:** both themes, 375/1280 widths, 8 colour pairs measured — **0 AA failures**, focus visible on every control, no horizontal overflow, 0 console errors. Original text follows. **Help / Support / Contact surface.** No `/help`, `/faq`, `/support`, `/contact` or `/report` route exists. `prd.md` tags launch-blocking. Compounding: the claim emails and the no-self-service-removal policy both route people "through Help/Contact" — a promise pointing nowhere, and with `MB6` now post-launch the privacy policy's manual-deletion route lands there too. **SCOPE DECIDED 2026-08-23: one static `/help` page** — FAQ (students / clubs / accounts & privacy) + Contact block + "Report a problem" mailto. No ticket table, no new email path. |
| ⏸️ **MB6** | Trust | **POST-LAUNCH by decision 2026-08-23.** Self-service account deletion — users cannot delete their own account. Not a launch blocker. **Consequence: `/privacy` must keep stating deletion is a manual request to the support address, which makes `MB7`/`UX4` (a support address that actually receives mail) more important, not less.** From `prd.md:283-303`. |
| ✅ **MB7** | Trust | **DONE 2026-08-23** — `/privacy` now prints `zothub.uci@gmail.com` as a real `mailto` anchor (it was previously `privacy@zothub.app`, a non-existent mailbox, styled blue but not actually a link). Uses `text-accent-text`, measured at 8.45:1 light / 9.88:1 dark. Shipped with `MB4`. |
| ✅ **MB1** | — | **CLOSED 2026-08-11 — was a false alarm.** The unique constraint exists in production (verified). The `implementation-audit.md:815` "two applications to opp 3" observation was a misreading: **Test Club has two *different* opportunities both titled `opp 3`** (see Phase 0 Q5 / the `opp 1/2/3/3/5` list), so one student applying to each looked like a duplicate. No work needed. |
| ✅ **D2** | — | **CLOSED 2026-08-23 — already fixed by commit `f82866f`, the same commit that wrote this item.** Verified: `README.md:87` says "the 6 Edge Functions", `:96` states student auto-approval, and the Turnstile vars are documented at `:91`, `:228`, `:235`, `:237`. **But a different README lie was found and is NOT closed — see `D4`.** Original text: **README was stale in three ways:** says signup needs manual admin approval (students auto-approve since S3); says "the 4 Edge Functions" (there are **6** — add `submit-club-claim`, `review-club-claim`); documents none of `VITE_TURNSTILE_SITE_KEY` / `TURNSTILE_SECRET_KEY` / `CAPTCHA_DISABLED`. **A fresh deploy missing the Turnstile site key hard-blocks signup** — this nearly happened on 2026-07-27. |
| ✅ **P1-license** | — | **CLOSED 2026-08-23 — the maintainer took the photo and owns it.** `public/images/hero-campus.jpg` requires no licence work and no replacement. (`P1`, the WebP/AVIF conversion, is a separate open item.) |

### UI/UX (Phase 2 — being scoped)

| ID | Impact | Item |
|---|---|---|
| 🔄 **UX0** | UX | **Usability + consistency pass — the next phase.** Maintainer's defect list is logged in full below (`UX1`–`UX11`), plus siblings found by generalising each fault (`UX12`–`UX17`). **Goal: bring every page to the quality bar the landing page already meets.** See [Phase 2 — UI/UX defect log](#phase-2--uiux-defect-log). |
| 🔄 **MB5-logo** | UX | **READY FOR THE MAINTAINER TO RUN, 2026-09-21.** `scripts/rehost_club_logos.mjs` is written and tested. **The manifest checked out exactly**: 724 seeded records (+ Test Club = the 725 in production), **589 logo URLs, and all 589 resolve** — zero dead, all on `zotspot.uci.edu`, all distinct files (23 of 23 sampled had unique hashes, so no shared placeholder). Verified in two layers: `tests/scripts/rehost-logos.test.mjs` runs the real script against a stand-in Supabase (14 checks — canary abort touches nothing, already-done clubs are skipped, per-club 320px fallback is counted, an HTML page is refused, a dry run writes nothing, and an upload whose row update fails is reported rather than counted as done); then a **real** dry run against the real manifest and the real ZotSpot did 25 clubs with 0 failures and **0 storage writes and 0 row updates**. **Runbook:** `SUPABASE_URL=… SUPABASE_SERVICE_ROLE_KEY=… node scripts/rehost_club_logos.mjs <manifest.json> --limit=5 --commit` first, check the five clubs, then re-run without `--limit`. Re-running is safe and resumable — clubs that already have a `logo_url` are skipped. Original status follows. | **IN PROGRESS 2026-09-21.** `scripts/verify_logo_manifest.mjs` is written and tested — read-only, it checks a supplied manifest's shape against what production already holds and reports how many logo URLs still resolve. **Waiting on the maintainer to supply the manifest**, which is not in the repo. **One lesson already banked from testing it against a planted file: a missing image can answer `200` with `text/html`, not `404`** (any SPA host does this), so counting HTTP status alone would have reported error pages as usable logos and the bulk job would have faithfully re-hosted them. The headline number is therefore "resolves AND is an image", never "resolves". ⚠️ **Blocker (b) below is now DOUBTED and must be settled before it is worked around.** It claims the `auth.uid() = foldername[1]` storage policies block a service-role write for seeded clubs (whose `user_id` is NULL). But `scripts/migrate_storage.ts` already uploads into `club-assets` with a **service role key**, at paths from a different project, and that migration was performed — service_role bypasses RLS, so the policy is probably irrelevant to this job. **Not verified either way** (it needs a local stack or a live attempt), so the re-host script will open `--commit` mode with a single canary upload-and-delete and abort loudly if it fails, turning the unknown into a safe self-check rather than an assumption. Original text follows. | **Re-host club logos — split now confirmed.** 725 clubs, **0** with a hosted logo. **589 are re-hostable** from `source_logo_url`; **136 have no source logo** and will keep initials permanently (so design the initials fallback to look deliberate, not broken). Re-host into a storage bucket and set `logo_url`. Highest-visibility single fix on the site. **APPROVED 2026-08-23** — see [Decisions made](#decisions-made). ⚠️ **Rescoped: club logo upload already ships** (`ClubProfileSetup.tsx:271-282`). The two real gaps are (a) the bulk re-host script, never written — `seed_clubs.mjs:65` explicitly defers it — and (b) **a service-role write path for seeded clubs, whose `user_id` is NULL** (`20260727000100:29`), so the existing `auth.uid() = foldername[1]` storage policies cannot authorise the write. Fix (b) before writing the script. Also depends on `S9` (the bucket has no mime/size limits). |
| ⏸️ **MB2** | UX | **POST-LAUNCH by maintainer decision, 2026-09-21.** Students keep the initials avatar for launch; no photo upload ships. Nothing is half-built as a result — `StudentProfileEdit` has no avatar control today, so there is no dead UI offering a feature that does not exist, and `avatar_url` stays read-only until this is picked up. **Consequence that now matters more:** the initials avatar is the permanent launch treatment for **every student** as well as the 136 logo-less clubs, so it has to look deliberate rather than like a failed image load. Context gathered while deciding: student profiles are readable by **signed-in users only** — verified against production, where an anonymous read of `student_profiles` returns `[]` while a control read of `opportunities` returns rows — so a public photo bucket would have made the picture *more* exposed than the name beside it. Original text follows. | **Student avatars are read but never written.** ⚠️ **Rescoped 2026-08-23: "needs a bucket + RLS" is false** — buckets and 9 RLS policies already ship (`20251223165608:2-77`) and a working uploader exists (`src/components/ui/file-upload.tsx`). What is actually missing: an avatar field on the edit form, one line in the upsert at `StudentProfileEdit.tsx:137`, and the deferred public-vs-private bucket decision. Note `A1` means OAuth users have no profile row at all, so avatars are doubly blank for them. |
| ⬜ **MB8** | UX | **Onboarding polish** — profile-completeness indicator, guided checklist, post-login return-to-context. From `prd.md`, previously untracked. |

### Security

| ID | Impact | Item |
|---|---|---|
| ✅ **S2** | — | **CLOSED 2026-08-11 — audit run, no abuse found.** One admin only (`zothub.uci@gmail.com`), expected, no secondary role, granted minutes after account creation. The self-grant loophole (closed by `S1`) was never used. Phase 0 security posture is now fully clear. |
| 🔄 **S5** | Security | **FIXED IN THE REPO 2026-09-21 — NOT YET DEPLOYED.** `send-reminders` no longer imports Resend at all (0 references); every one of its four passes now goes through `send-email`, whose templates escape every interpolation. Needs an edge-function deploy by the maintainer. **A bug was found and fixed in the template it now routes through — see `UX25`: the new-post unsubscribe link switched off the wrong preference, and the inline HTML being replaced had it RIGHT, so this fix would otherwise have regressed a working opt-out.** Original diagnosis follows. | **`send-reminders` emails are not HTML-escaped.** It interpolates club names, event/opportunity titles and `full_name` raw into `html` (lines 81-113, 181-206, 292-317, 388-414). 724 club names were **scraped from ZotSpot** — third-party text we never sanitised — and go out from the verified `zothub.app` domain. Fix = route through `send-email`, which already escapes. |
| ✅ **S6** | — | **CLOSED 2026-09-19 — it is the ANON key, not the service role. No exposure, no action.** Verified in production via `scripts/check_cron_token_role.sql`, which decodes only the `role` claim from the JWT so the token itself is never printed: `send-reminders-hourly` (jobid 1, `0 * * * *`, active) → `token_role = anon`; `archive-past-events-nightly` (jobid 2, `0 9 * * *`, active) → no bearer token at all. The anon key is public by design and already ships in the browser bundle, so a readable copy in `cron.job` discloses nothing. **No rotation needed.** `R2` (versioning the schedule in a migration) is unaffected and still open. Original concern follows. | **A bearer token is stored in plaintext inside the cron job definition.** Q4 shows the `send-reminders-hourly` command embeds `'Authorization', 'Bearer eyJhbGciOi…'` directly in the SQL. Anyone who can read `cron.job` can read that token — and if it is the **service-role** key, that is full database authority sitting in a readable table. **Confirm which key it is**, then move to `vault`/a DB setting and rotate if it was the service role. Handle as part of `R2`, since that migration rewrites the job. |
| ⬜ **S4** | Security | **Unsalted SHA-256 password** held in `email_verifications.password_hash` during the ~10-minute OTP window (`send-otp/index.ts`). Transient, and the real credential lives in Supabase auth. Fix = salt + slow KDF, or stop storing it. |

### Reliability

| ID | Impact | Item |
|---|---|---|
| 🔄 **R1** | Reliability · UX | **FIXED IN THE REPO 2026-09-21 — NOT YET DEPLOYED.** Claim-before-send: the log row is written FIRST, the send is judged by `checkEmailResult`, and the claim is RELEASED when delivery fails so the next hourly run retries. The ordering rule is extracted to `supabase/functions/_shared/reminder-delivery.ts` — plain TS, no runtime APIs, same pattern as `email-result.ts` — and **unit-tested from Node: 8 checks, and restoring the old send-then-log order fails exactly 3 of them**, the three describing the bug. Deno is not installed here, so extracting the rule is what made the dangerous part testable at all rather than buried in a handler nothing can run. Claiming first also makes overlapping cron runs safe: the unique constraint decides which run owns the send. Original diagnosis follows. | **`send-reminders` marks failed sends as delivered.** It only catches thrown exceptions, so a Resend `{ error }` response still writes the `reminder_logs` row — and the `unique_reminder` constraint then makes that reminder **permanently unsendable**. Students silently never get it. Fix = claim-before-send + `checkEmailResult` + release the claim on failure. |
| 🔄 **R2** | Reliability | **BLOCKED ON ONE READ-ONLY QUERY FROM THE MAINTAINER.** `scripts/inspect_reminder_cron.sql` is ready: it prints every cron job (with the bearer token masked so the output is safe to paste) plus the last 10 run results. It is needed BEFORE the migration is written, not after: the job exists only as production state, so re-scheduling blind risks either double-sending or silently killing all reminder email, and neither is discoverable afterwards. The run-details half matters too — a job that exists but fails every hour looks identical to a healthy one in `cron.job` alone. Original diagnosis follows. | **Reminder cron schedule is unversioned.** No `send-reminders` entry in `config.toml`, no `cron.schedule` in any migration — it exists only as prod state ("scheduled manually, out-of-repo", per `20260713000100…sql:5-18`). If lost, *all* reminder email stops silently. Pass 3 also hard-codes a 1-hour lookback, so any pause skips posts permanently rather than delaying them. Fix = commit the schedule. **Live job identified: `send-reminders-hourly`, schedule `0 * * * *`, active (jobid 1)** — the migration must `cron.unschedule('send-reminders-hourly')` before re-scheduling, or it will double-send. |
| ⬜ **MB3** | Reliability · UX | ⚠️ **`Clubs.tsx` is now CACHED but still unbounded** — the ~725-row read happens once a minute instead of once a page view, and the landing page no longer duplicates it, but the payload is unchanged. Client-side filtering is deliberately retained (the corpus is in memory, so filtering is now instant); a `useDeferredValue` debounce is the orthogonal fix, and server-side search is a separate decision. **Hard `.limit(50)` and client-side substring search.** ⚠️ **Corrected 2026-08-23 — this item had the facts backwards.** Real limits are at `Opportunities.tsx:113`, `Events.tsx:104`, and an unlogged fourth at `useNotifications.ts:47`. **`Clubs.tsx:38` has NO limit** — it calls `rpc("get_all_clubs_public")` unbounded, pulling **~725 rows on every visit** and re-filtering them on every keystroke with no debounce; `Landing.tsx` calls the same RPC just to read `.length` on the LCP page. So "fine at current volume" is inverted: the *capped* pages hold ~5 rows, the *uncapped* one holds 725. Also: PostgREST `max-rows` is a dashboard setting with no in-repo override, so the Clubs companion count queries may already be silently truncating. |

### Verification — code exists, never exercised with real data

Needs test accounts: a student with applications/RSVPs/saves/follows, an admin, a second
student (capacity tests), ≥1 club team member. **Several are blocked by D1.**

| ID | Item |
|---|---|
| ⬜ N1 | Populated club Team row — `TeamManagement.tsx`, `ClubDetail.tsx` (blocked by D1) |
| ⬜ N2 | Messages MEMBER chip — `useMessages.ts:108-119`, `ConversationList.tsx:75-77` (depends on N1) |
| ⬜ N3 | Student Messages + Activity with real apps/RSVPs/saves/follows |
| ⬜ N4 | Waitlist pending (+30s poll) and rejected-with-reason screens |
| ⬜ N5 | Admin dashboard — approve / reject-with-reason / delete, stats, search, filter |
| ⬜ N6 | Event at-capacity guard — 2nd student RSVPing a capacity-1 event |
| ⬜ N7 | Profile "still missing" nudge on a fresh/incomplete profile |
| ⬜ N8 | **Decision, not a check** — RSVP emails preference-gated or not? `prd.md:134` contradicts `prd.md:139`. |

### Research — unvalidated assumptions

| ID | Impact | Item |
|---|---|---|
| ⬜ **RS1** | UX | **Club recruiting walkthrough + artifacts.** Never obtained. Blocks confident design of the review pipeline and the application-review screen — currently built on assumption. Source: `docs/strategy/02-research.md` §4. |
| ⬜ **RS2** | UX | **Student interviews / assumption A5** — do students already discover clubs via ZotSpot? Decides the student-side headline ("finally, one place" vs "the same clubs, without the friction"), i.e. the landing page. Interview guide is written and ready to run. Source: `docs/strategy/02-research.md` §3. |

### Polish / housekeeping — nobody notices these

| ID | Impact | Item |
|---|---|---|
| ⬜ **P1** | UX | Hero → WebP/AVIF (LCP element, ~233 KB). Blocked on `cwebp`/`avifenc`. Separate from `P1-license`. |
| ⬜ **DP3** | Housekeeping | Delete dead asset `src/assets/hero-bg.jpg` (172 KB, zero importers) |
| ⬜ **DP4** | Housekeeping | Delete dead code: `dashboard/DashboardLayout.tsx`, `NavLink.tsx`, `pages/Index.tsx` |
| ⬜ **DP5** | Housekeeping | Drop `@deprecated` aliases with no consumers — `src/types/index.ts:23,26,38,41` |
| ⬜ **H1** | Housekeeping | Dead `club_followers` table — declared in `types.ts:114`, never queried (following uses `bookmarks`) |
| ⬜ **H2** | Housekeeping | Lovable decommission — 8 unchecked boxes, `plan.md:411-428`. No longer serves traffic. |
| ⏸️ **DP6** | — | `verify-otp` writes a club_name placeholder (`email.split("@")[0]`). Deferred by decision. |
| ⏸️ **DP7** | — | RSVP-journey realtime enhancement. Deferred. |
| ⏸️ **DP8** | — | Accent-coloured sent message bubbles — cosmetic, "toneable on request". Deferred. |
| ⏸️ **DP9** | — | Maintainer's pending UI tweaks + design-archive housekeeping. Likely absorbed by UX0. |

---

---

## Phase 2 — UI/UX defect log

**Status: LOGGED, NOT IMPLEMENTED.** Captured 2026-08-11 from the maintainer's review pass,
each item verified in code with a root cause, then generalised to find the same fault
elsewhere. **Nothing here has been changed yet.**

The organising goal: **the landing page is the quality bar.** Every other page should match
it for polish, consistency and honesty. Most items below are not isolated bugs — they are
one inconsistency repeated across pages, which is why they are grouped by root cause.

### Reported — bugs

| ID | Impact | Item, root cause, and scope |
|---|---|---|
| ⬜ **UX1** | UX | **Navigation feels like the old page never left.** Clicking a link keeps the current shell mounted while the new page's components and data stream in, so it reads as slow. **Root cause:** every page hand-rolls `useEffect` + `useState` + its own `isLoading` skeleton; there is no route-level data gate. Wanted: navigate → resolve → show the finished page. **Scope: 21 of 31 page files** (corrected 2026-08-23 from "17"). ⚠️ **The proximate cause was also missing from every doc:** `App.tsx:77` sets `future={{ v7_startTransition: true }}`, which keeps the *outgoing* page painted and bypasses `RouteFallback` — that flag is the switch most directly controlling the symptom. `Landing.tsx:21-38` has no loading gate at all. See also `UX15`, `UX14`. |
| ✅ **UX2** | UX | **Events is unreachable from Discover.** The navbar has a single "Discover" item → `/opportunities`, whose `match` *also* highlights on `/events` — so Events looks like part of Discover but there is no link to it (`Navbar.tsx:12`). Only routes in from the landing-page footer or a typed URL. There is **no `/discover` route at all**. **DECIDED 2026-08-23: neither — Opportunities and Events become two separate top-level nav items.** No `/discover` hub, no segmented switcher. Implement together with `UX6`, which frees the slot. |
| ⬜ **UX3** | UX | **No back affordance on entry pages.** `Login.tsx` has none. **Generalised — also dead-ends:** `Waitlist.tsx` and `WaitlistRejected.tsx` have none either. ⚠️ **Corrected 2026-08-23: `Signup` does NOT already have one.** `Signup.tsx:329-335` is an in-page `setStep("role")` button; the `step==="role"` first screen (`:253-306`) has no back control at all. Also `Waitlist`/`WaitlistRejected` have no exit except **Sign Out**, which destroys the session. (`ForgotPassword`, `ResetPassword`, `Privacy`, `Unsubscribe`, `NotFound` do have one.) Add a consistent top-left back control. |
| ⬜ **UX4** | Trust | **Unreachable email published.** `Privacy.tsx:145` advertises `privacy@zothub.app`, a mailbox that does not exist → change to `zothub.uci@gmail.com`. Audited the whole app: this is the **only** unreachable address (`notifications@zothub.app` is the legitimate Resend sending domain). Overlaps `MB7`. **✅ DONE 2026-08-23 with `MB7`.** |
| ⬜ **UX5** | UX | **Remove the homepage live counts** ("x open roles · y upcoming events · z clubs") — `Landing.tsx:153-158` + the `useLiveCounts` hook at `:21-57`. Note the counts are currently *honest but unflattering*: 5 opportunities (all Test Club junk) and 0 events. Removing them also removes 3 queries from the landing critical path. |
| ✅ **UX6** | UX | **Messages belongs in the top-right icon row**, between notifications and profile, for **both** roles. Today Messages is a tab in `TabBar` (`navConfig.ts:55,82`). ⚠️ **Corrected 2026-08-23: "notifications is not an icon at all" is WRONG.** `TopNav.tsx:78-99` already renders a bell with an unread dot and a counted `aria-label` — *in addition to* the `AccountMenu.tsx:65-70` dropdown row. So the real work is **de-duplicating** the two notification entry points and moving Messages in beside the bell, not building an icon from scratch. (`design-system.md:118` is the accurate doc here; this item was the wrong one.) So this is a small nav restructure: notifications + messages as sibling icons, profile menu third. **DECIDED 2026-08-23 — confirmed, and paired with `UX2`:** Messages leaving the nav row is exactly what frees the slot Events needs, so the row becomes **Opportunities · Events · Clubs · Activity** and the design system's four-destinations rule still holds. Icon order, left→right: notifications, messages, profile initials. |
| ✅ **UX7** | UX | **DONE 2026-09-12.** Both root causes fixed. (a) `useProfileLookup` kept its "cache" in component-local `useState<Map>`, so it cached **nothing** across navigation — and the nav remounts per route. Worse, `fetchProfileInfo` listed that cache in its deps, so every cache write produced a new callback identity and re-ran every effect depending on it, including `useAccountIdentity`'s — the hook's own comment claiming it was cached was the opposite of true. It now reads through the shared TanStack Query cache, which survives unmount. (b) `StudentTopNav.tsx` and `ClubTopNav.tsx` both dropped `isLoading`; both now destructure and forward it, `TopNav` threads it, and `AccountMenu` renders a themed skeleton (correct shape per the design system — circle for a person, rounded-square for a club) plus an `sr-only` "Loading your account" instead of the email-derived initials. Original text follows. | **Avatar initials flash the wrong initials on every navigation** (`mantrid@uci.edu` → "MA", then "DM" once loaded). **Root cause:** `useAccountIdentity` deliberately exposes `isLoading` and its own comment says prominent surfaces should skeleton — but `ClubTopNav.tsx:14` and `StudentTopNav.tsx:12` destructure only `{ displayName, subtitle, avatarUrl }` and **drop `isLoading`**, rendering the email-derived fallback. `club/ClubHome.tsx:97` *does* honour it — so the codebase is inconsistent. It repeats on every route because the state is component-local and the nav remounts per page. |
| ✅ **UX8** | UX | **FIXED 2026-09-21.** Every signed-in address now matches its nav label, with a redirect from each old one — `LEGACY_ROUTES` in `App.tsx`, verified end to end by `tests/browser/ux8-url-renames.mjs` (27/27: each old address lands on its new home, each new one stays put and renders its own section, and `/messages` serves both roles). **Three bugs the rename created and the test caught, none visible in the diff:** (a) the old club paths were flat siblings under `/club/dashboard` while the new ones NEST, and `"/postings/events".startsWith("/postings")` is true — so `ClubHome.getSection` and `ClubSectionNav` both silently rendered the wrong section and lit the wrong tab; (b) `<Navigate to="/postings/:id/edit">` navigates to the LITERAL string `:id`, so a club following an old edit link landed on a posting called `:id` — a `LegacyRedirect` component now substitutes the params; (c) a plain `Navigate` also drops the query string and hash, which the same component now carries across. Original diagnosis follows. | **URLs are legacy and contradict their labels.** Nav says "Activity" but the URL is `/student/dashboard`; "Messages" is `/student/messages`. Needs one canonical scheme decision (e.g. `/activity`, `/messages`) with redirects from the old paths — note `/club/feed` and `/student/feed` already have redirect precedent (`App.tsx:109,221`). Audit every role-scoped path together, not one at a time. |

### Reported — design

| ID | Impact | Item, root cause, and scope |
|---|---|---|
| ✅ **UX9** | UX | **FIXED 2026-09-21** with `UX10`/`UX12` in one sweep. `Landing.tsx` resolves both split-card targets from the auth state; see the decision below. Proven by `tests/browser/ux9-cta-sweep.mjs`, which **clicks** each CTA in all three auth states and asserts where the browser comes to rest — deliberately not an href check, because the href was always a real route and the fault lived in the redirect one hop later. Original text follows. | **"Bring your club to ZotHub" sends signed-in users to the wrong place.** `Landing.tsx:185` → `/signup?role=club`, and `Signup.tsx:57-63` bounces any authenticated user to `/club/dashboard` (Responses) or `/student/dashboard` (Activity). Wanted: signed-in → clubs view; signed-out → signup. |
| ✅ **UX10** | UX | **FIXED 2026-09-21** — see `UX9`. Signed in, it goes to `/clubs`; signed out it still starts the student signup, which the maintainer chose to keep. Original text follows. | **"Explore clubs" has the same fault.** `Landing.tsx:201` → `/signup?role=student`, same bounce. Wanted: **always** → the clubs page, regardless of auth state. |
| ✅ **UX11** | UX | **FIXED 2026-09-21.** One `DiscoverToolbar` (`src/components/discover/DiscoverToolbar.tsx`), adopted on all three list pages. Shape per the maintainer's decision below: search · viewer-scope chips · `Filter` menu · sort · card/list toggle, one line on a laptop, wrapping (never horizontally scrolling) on a phone. **Verified in a browser by `tests/browser/ux11-toolbar.mjs` (24/24)** and the two highest-stakes checks proved IN REVERSE — breaking each one makes its test fail with the real symptom. **Three things the tests caught or forced that reading the diff would not:** (a) Radix closes a menu on every item activation, so without `onSelect: preventDefault` picking two types meant opening the menu twice — the one capability this redesign was for, silently reverted; (b) shadcn's `DropdownMenuCheckboxItem` draws **nothing** in the indicator slot when unchecked, so every unticked option read as a plain link to somewhere rather than a box to tick — an explicit empty box was added; (c) the sort had to be asserted **on the wire**, not on the screen, because a client sort and a server sort are indistinguishable on a list of three rows and only diverge past the 50-row cap. Original text follows. | **Unify the list-page toolbar.** Clubs / Events / Opportunities should each present **search · filter · sort · card-list toggle on one line**, in the same order, with **filter as a collapsible multi-select** like sort — instead of today's chip row spilling below. Shared pieces already exist to build on (`components/discover/`: `FilterChip`, `ViewToggle`, `DiscoverList`, `EmptyState`) but there is **no shared toolbar component**, which is why the three drifted. |

### Found by generalising the above

| ID | Impact | Item |
|---|---|---|
| ✅ **UX12** | UX | **FIXED 2026-09-21, and the sweep it asked for was done.** Every `Link`, `Navigate`, `navigate()` and internal `<a href>` in `src/` was inventoried against its label and checked in all three auth states. Three real faults: the logged one (**"Browse clubs"** → `/opportunities`, target corrected to `/clubs`), plus **two stale labels our own 2026-09-20 rename created** — the home-page footer and a role page's back link both still read **"Discover"**, which has not been the name of that page since. **Three findings logged rather than fixed here, as out of scope:** `DP12` (dead code with broken links), `A5` (a signed-in user with no role reaches a student page), `A6` (Signup has no waitlist branch where Login does). Original text follows. | **Another mislabelled CTA:** `Landing.tsx:149` renders a button reading **"Browse clubs"** that links to **`/opportunities`**. Same class as `UX9`/`UX10`. Sweep every CTA label against its target. |
| ✅ **UX13** | UX | **CLOSED 2026-09-21, both gaps built rather than argued away.** Events gained a sort (`Soonest first` / `Most popular`) and Clubs gained the card/list toggle (`src/components/clubs/ClubList.tsx`). The Events sort is **server-side and in the query key**, which is what contract `O3` reserved — and the same fault was found and fixed on Opportunities, where `Closing soonest` and `Most applied to` had been re-ordering the 50 newest roles in the browser. **Verified read-only against production** that an anonymous visitor can `ORDER BY` the two trigger-maintained counter columns — they carry only a column-level grant, so this was the step most likely to work locally and 400 in production; a control request ordering on a non-existent column returns `42703`, so the 200s mean something. That probe also corrected a false claim in my own code comment: Postgres already orders nulls **last** on an ascending sort, so `nullsFirst: false` is documentation, not a fix. Original text follows. | **Toolbar feature gaps, measured:** Opportunities has search+sort+view+filter; **Events has no sort at all**; **Clubs has no card/list toggle**. Fixing `UX11` must close these, not just restyle. |
| ✅ **UX14** | UX | **DONE 2026-09-12** — `useScrollRestoration` (new hook, mounted inside `BrowserRouter` since it needs router context). Forward navigation returns to the top; back restores the remembered offset; `#hash` links are left alone. React Router's `<ScrollRestoration>` was not an option — it requires a data router and this app uses `<BrowserRouter>` + `<Routes>`. **Three bugs were found by testing in the browser that reading could not have caught**, all fixed: (1) restoring on a single animation frame clamps to whatever height exists at that instant (700px landed at 386px), so it now re-applies across frames until the document has grown, bailing out if the visitor scrolls themselves; (2) React cleanup never runs on a full page load, so the position was lost entirely — a `pagehide` listener covers that; (3) **the real one** — reading `window.scrollY` at teardown reads it AFTER the new route has laid out, so the browser had already clamped it to the *new* page's shorter height and **386 was being saved, not 700**. Position is now tracked continuously by a passive rAF-throttled scroll listener, so the stored value was always real on the page it belongs to. Persisted in `sessionStorage` (per-tab, cleared on close) inside try/catch, since it throws outright in some privacy modes. Original text follows. | **No scroll restoration.** Nothing in `src/` resets scroll on route change, so navigating from halfway down a list lands you halfway down the next page — a direct contributor to the "page never changed" feeling in `UX1`. |
| ✅ **UX15** | Reliability · UX | **TanStack Query is installed and wired but completely unused — 0 `useQuery`/`useMutation` calls in `src/`.** `App.tsx` creates a `QueryClient` and wraps the tree (`:54,64`), then every page refetches by hand with no cache, no dedupe, no shared loading state. This is the architectural root cause behind `UX1` and `UX7`. **✅ COMPLETE 2026-09-19 — all five waves.** Wave 4 finished it: `ClubDetail` (4a, four serial reads become four parallel queries, plus `M17` which wave 2 had missed), `OpportunityDetail` (4b) and `EventDetail` + `useEventRSVP` + `RSVPForm` (4c) — the last two closing `UX21` on both sides. `M11` resolved by giving `useEventRSVP` sole ownership of every student-side `rsvps` write: the form now hands its formatted answers up, and the `requires_approval ? "pending" : "confirmed"` expression that existed in three places is computed once. `M12` extended beyond the contract — `RSVPReview`'s approve/decline changes the confirmed attendee count the club's own browser has cached, so both the single and bulk paths now invalidate. **64 browser checks across seven scripts, all green**, with the two highest-stakes fixes proved in reverse. Waves 0 (key registry + `queryClient.clear()` on sign-out) and 2 (Clubs + Landing, which had to ship together because they share `clubKeys.list()`) are complete. **Measured in a browser, not assumed:** a cold `/clubs` visit makes exactly 3 requests; navigating to the landing page and back again makes **0**. **Wave 1a/1b also done:** `useStudentProfileId` (the same `select("id")` was duplicated verbatim across **8** call sites — opening a role and applying ran it three times) and `useBookmarks` (now a cached query + mutation, mounted on 5 pages, with the duplicate-insert idempotency and the club Follow/Following wording preserved). **Wave 1 is complete** (1a `useStudentProfileId`, 1b `useBookmarks` + `A3`, 1c `useNavigationCounts` + `R3`). **Wave 3 is complete (2026-09-19):** `Events.tsx` on `eventKeys.upcoming()` and `Opportunities.tsx` on `opportunityKeys.list()`, plus the club-side invalidations `M4` (`CreateOpportunity`), `M5` (`EditOpportunity`), `M6` (`useClubOpportunities` delete), `M7` (`CreateEvent`), `M8` (`EditEvent`), **an unlisted M6 mirror in `useClubEvents` delete** (the contract's map missed it — a cancelled event would otherwise have stayed RSVP-able on the public list for a minute after its attendees were emailed that it was off), and **`M3` brought forward from wave 4b on purpose** (`ApplicationForm`), because caching the applied-roles Set without it would have shown "Apply" again on a role the student had just applied to. The applied-roles read is now ONE key (`applicationKeys.byStudent`) serving both the list badge and the detail page's `hasApplied` (contract C5). Both pages also gained a real `ErrorState` and the `UX22` sign-in prompt. **Measured in a browser, twice — signed out (16 checks) and as a signed-in student (9 checks).** The counter watches the `events` / `opportunities` / `applications` / `student_profiles` REST endpoints specifically, not total page requests: a cold `/events` issues **1** `events` request and a cold `/opportunities` **1** `opportunities` request; a client-side navigation away and back issues **0** further requests to any of them and shows **no skeleton**; and the Applied badge survives the round trip. Navigation is by link click and history, never `page.goto()` — a full document load wipes an in-memory cache by definition and would make the test pass or fail for the wrong reason. Two small conversions outside the contract's wave-3 scope, both load-bearing: the empty-state CTAs on these pages moved from `<a href>` to `<Link>`, because a full document reload from inside a page discards the entire cache this change exists to build. Remaining: wave 4 (the three detail pages), which is where `UX21` is mandatory. **Three carry-forwards from the wave-3 review, so they are not lost:** (a) `ApplicationForm.tsx` still hand-rolls its own `student_profiles` lookup instead of using `useResolveStudentProfileId()` — the third surviving duplicate of the read wave 1a existed to delete; fix it in 4b, which touches the same lines anyway. (b) `M3` does not invalidate `opportunityKeys.byClubPublic(clubId)`; harmless today because `ClubDetail` is not cached yet, but the applicant count renders there too — 4a must not inherit the contract's omission. (c) The events list now caches the embedded `rsvps` array, so a card's attendee count is stale after your own RSVP until `M9`/`M10` land in 4c. **Foundation landed 2026-09-12.** `App.tsx` now configures `staleTime: 60s`, `gcTime: 5m`, `refetchOnWindowFocus: false`, `retry: 1` (the step-zero fix — a bare `new QueryClient()` inherits v5's `staleTime: 0`, so adoption without it changes nothing a user can feel). The identity slice is migrated: `useProfileLookup` exposes a declarative `useProfile()` plus stable-identity imperative helpers backed by `fetchQuery`/`setQueryData`, preserving the batch N+1 fix. **DECIDED 2026-08-23: adopt it properly, across all pages.** ⚠️ **Critical implementation note:** `App.tsx:54` is a bare `new QueryClient()`. Adopting TanStack Query **without setting `defaultOptions.staleTime` inherits v5's `staleTime: 0` and will NOT fix the symptom** — pages will still refetch on every mount. That one line is step zero. Partial adoption was explicitly rejected — an inconsistent codebase is what produced this defect log. **This lands first**, before any per-page `UX*` work, so pages are not rewritten twice. |
| ⬜ **UX16** | UX | **Landing-page copy/behaviour is the reference; other pages have not been re-checked against it** since the redesign. Phase 2 should walk both journeys (student: signup → discover → apply → messages; club: signup → post → review → team) in **light and dark** and at **mobile widths**, and file what it finds here. Overlaps the never-exercised screens `N1`–`N7`. |
| ✅ **UX17** | UX | **CLOSED 2026-09-21 — all three parts done.** **17a:** the two empty discovery pages used to promise each other (Opportunities said "events are worth a look", Events said "roles are open though") — a visitor on an empty site went in a circle between two empty pages. Both now render the shared `NothingYetState`, which reads the **live** club count off the same cached query the directory uses (no extra request, never stale) and points at the clubs — measured on production as the only surface with content on launch day: **725 clubs, 4 test roles, and 0 publicly visible events, ever**. **17b:** the Clubs empty state's three stage-copy lines are gone; an empty 722-club directory is treated as the oddity it would be, with a retry. **17c:** already done. Also: the result count is now hidden at zero on both pages — the empty state below says it better than "0 roles" above it. Proven by `tests/browser/ux17-empty-states.mjs` (15 checks), and restoring the old copy fails 3 of them. Original text follows. | ⚠️ **Rescoped 2026-08-23 — the original framing was wrong.** `EmptyState` *is* used consistently with branch-aware copy on all three pages (`Clubs.tsx`, `Events.tsx`, `Opportunities.tsx` — line refs deliberately dropped, they went stale twice), so "undesigned empty states" is false. The three real defects are: **17a — the zero-states make false claims and form a loop** (`Opportunities.tsx:367` promises events; its CTA at `:391` goes to `/clubs`; "roles are open though" links to an empty page). **17b — `Clubs.tsx:231-236` breaks the design system's no-stage-copy rule three times.** **17c — ✅ FIXED on the three list pages (Clubs 2026-09-19 wave 2; Events + Opportunities 2026-09-19 wave 3).** Each now renders `ErrorState` (`src/components/discover/ErrorState.tsx`) on `query.isError`, checked **before** the empty branch, so a network failure can never render "No clubs yet" / "No events coming up" / "No open roles right now". Verified in a browser by failing the read and confirming both the distinct copy and that **Try again** re-issues the request and recovers. **Still open for the detail pages** (`ClubDetail`, `OpportunityDetail`, `EventDetail`) — wave 4. Original: a failed fetch rendered byte-identically to an empty result because the `catch` blocks only `console.error`; a broken database would have looked exactly like an empty one on launch day. Original framing: **Empty states will read as broken, not empty.** ⚠️ **Design against the post-purge reality, not today's screen.** `D1` is deferred to the final pre-launch step, so the site *currently* shows 5 junk opportunities — but on launch day, once they are removed, discovery is **0 opportunities and 0 events**. The empty state is therefore the *default* launch experience and must be deliberate. Do **not** wait for `D1` to design it. Interacts with `UX5`. |

---

> ⚠️ **Doc corrections still owed after the 2026-08-23 audit** (per `CLAUDE.md` § Documentation
> discipline — "if two docs disagree, the backlog wins, then immediately correct the loser"):
> `HANDOFF.md` §1 still lists `MB6` as a launch blocker, §1/§2 still treat `D1a` as live, §3(a)
> still poses `UX15` as an open question, §6 still says "No `CLAUDE.md` exists" and "clean at
> `6c69a8d` plus unstaged changes", and §2/§5 still assert 115/115. `CLAUDE.md` still says
> `send-reminders` is "the one email path" that bypasses the checker (see `E1`) and repeats the
> 115/115 claim (see `T2`). `prd.md` still tags `MB6` launch-blocking (`:312`) and says four
> Edge Functions (`:136`). `docs/strategy/03-structure-decisions.md` §2/§3 and
> `design-system.md:118` still state the `/discover` hub and "Discover · Clubs · Activity ·
> Messages" as agreed — **and `StudentTopNav.tsx`/`ClubTopNav.tsx` docstrings cite that
> superseded doc**, which will point the next implementer at the wrong decision.
> `docs/strategy/02-research.md` §5 lists heuristic findings the redesign already closed and
> carries no ARCHIVED banner.

## Decisions made

Recorded as the maintainer answers them. **Write the answer and the date here before building
anything on it** (`CLAUDE.md` § Documentation discipline).

| Date | # | Decision | Consequence |
|---|---|---|---|
| 2026-08-23 | **Scope** | **Full quality bar before launch.** Everything logged here — `UX1`–`UX17`, all launch blockers, plus whatever a fresh audit turns up. No date pressure; the app is finished first, *then* launch strategy is planned. | Nothing in this file is skipped on scope grounds. New audit findings are added, not triaged away. |
| 2026-08-23 | **D1 / D1a** | **No public traffic today** — nobody has the link. `D1a` (hiding Test Club's 5 junk opportunities) is therefore **not needed**; test data stays for `N1`–`N7` verification. `D1` still runs as the final pre-launch step. | `D1a` → ⏸️ deferred, not open. Test Club retained deliberately. |
| 2026-08-23 | **MB5-logo** | **Approved: re-host the 589 club logos** from `source_logo_url` into our own storage bucket. Rationale accepted — these are the clubs' own logos, publicly posted on the university's own club directory, shown on a UCI student platform to represent that same club. The 136 with no source logo keep initials permanently, so the initials fallback must look deliberate. | Unblocks the highest-visibility fix on the site. Needs a bucket + a re-host script + a prod write (see the access question below). |
| 2026-08-23 | **UX15** | **Adopt TanStack Query properly across all pages** — do not drop it, do not do a partial adoption. Partial adoption was explicitly rejected as "the exact thing that created this mess the first time." | Largest single workstream. Root-cause fix for `UX1` (slow-feeling navigation) and `UX7` (avatar initials flash). Must land before per-page UX work so pages are not rewritten twice. |
| 2026-08-23 | **UX2 + UX6** | **Nav shape decided.** **Opportunities** and **Events** become two separate top-level nav items (no `/discover` hub, no segmented switcher). **Messages moves out of the nav row into the top-right icon row**, between the notifications icon and the profile-initials icon. | Resolves `UX2` (Events was unreachable) *and* `UX6` together, and keeps the design system's four-destinations rule intact: Messages vacating the row is exactly what frees the slot Events needs → **Opportunities · Events · Clubs · Activity**. `UX6` also requires promoting notifications from an `AccountMenu` row to a real icon. |
| 2026-08-23 | **Club nav names** | **`Postings · Applicants · My Club`** (3 items; Messages moves to the icon row). "Applicants" is kept deliberately: **an event RSVP is treated as applying to the event**, so the word covers both. The page carries **two sections** — applicants for opportunities, and applicants via event RSVPs. | Corrects an earlier bad suggestion: a separate club Events nav item was **rejected** as duplication — `navConfig.ts:64` shows `Postings` already matches both `/club/dashboard/opportunities` **and** `/club/dashboard/events`. `UX19` (clubs have no discovery destination at all) still needs solving inside this nav work. |
| 2026-08-23 | **Reminder emails** | **Leave the `send-reminders-hourly` cron ON.** Do not touch production to disable it; fix `S5` (escaping) and `R1` (failed sends recorded as delivered) soon. | Accepted risk, and it is currently theoretical: with 0 RSVPs and 0 followers there is nobody to remind and no club text to inject. **Must be fixed before any real club or student is onboarded** — after that the failure is permanent per-student (`unique_reminder` makes a mis-logged reminder unsendable forever). |
| 2026-08-23 | **D3 / privacy copy** | **Delete the data-export sentence outright** (`Privacy.tsx:114`) rather than rewording it or building an export. | Removes a promise the product cannot keep. `MB7`/`UX4` (a support address that actually receives mail) still ships, so the manual route survives. Revisit an export when `MB6` is built post-launch. |
| 2026-08-23 | **A1 / Google signup** | **Fix it properly now** — do not hide the button, do not drop Google signup. Create the profile server-side, auto-approve students the way OTP already does (`S3`), and stop discarding the insert result. | Highest-priority code change in the phase. Touches `AuthContext.tsx:144-175` and needs either a service-role edge function or a role-grant-before-insert ordering; the RLS policies at `20251223013805:73`/`:108` are correct and should NOT be loosened. |
| 2026-08-23 | **/help copy** | **No timing promise for club review.** The FAQ states that a person reviews every club and claim, and offers an email route for an update, but commits to no turnaround. The question was reframed from "How long does club approval take?" to "What happens after I sign up or claim my club?" — a question that does not answer itself would itself be the dead end Foundation rule 1 forbids. | Nothing public to hold the maintainer to. `prd.md`'s "twice daily during beta" stays an internal operational commitment, not a published SLA. |
| 2026-08-23 | **Seeded-club takedown** | **A club that does not want to be listed can email and have it removed.** Stated plainly in `/help`. | The honest counterpart to seeding 724 listings from UCI's public directory without those clubs asking. Does not reintroduce *self-service* removal (still admin-reviewed by decision) — it is a manual, human-handled request. |
| 2026-08-23 | **Work order** | **Finish the trust-and-legal batch before anything else**, then security → CI → data layer → nav/URLs/toolbar/CTAs/empty-states → test-data purge. Doing the visible nav work early was explicitly rejected. | Half-finished batches are how the tracker became confusing in the first place. Nav must precede the CTA sweep or every CTA is rewritten twice. |
| 2026-08-23 | **S8 approach** | **Close the forged-notification hole by moving the work into the database**, not by adding an edge function and not by restricting users to their own account (which would have silently stopped attendees being told an event was cancelled). | Matches the pattern `notify_rsvp_status_change` and `notify_club_on_new_application` already use — event cancellation was the only notification still being written from the browser. Nothing a real user does changes. |
| 2026-08-23 | **S9 limits** | **`club-assets`: images + PDF, 10 MB per file.** PDF included so a club can upload an event flyer as a PDF. | Accepted tradeoff: a PDF can carry scripts, but ZotHub only ever links to these files and never renders them itself. A typical club logo is 20–80 KB, so 10 MB is generous headroom. |
| 2026-08-23 | **Migration delivery** | **One migration file, proven against a throwaway local database first**, then a single maintainer-run command. Not separate migrations, and not raw SQL pasted into the dashboard. | Keeps the schema history in the repo — pasting SQL straight into the dashboard is how the migration history got out of sync before. |
| 2026-08-23 | **Interaction** | **Keep asking — do not switch to autonomous judgement.** And **if `AskUserQuestion` fails or is blocked, ask again rather than proceeding on an assumption.** | Recorded in `CLAUDE.md` § Working with the maintainer. A tool glitch is never licence to guess. |
| 2026-08-23 | **Push order** | **Push the OAuth work to `main` before the edge function is deployed.** Rationale accepted: Google signup is already broken, so shipping the frontend first leaves it broken *differently* (a logged error instead of a silent failure) rather than regressing anything, nobody is visiting the site, and it unblocks `/help`, the privacy corrections and the theme fix immediately. | A deliberate, reasoned exception to the migrations→functions→frontend rule, not a lapse. **The rule still stands for every change with live users.** `provision-oauth-user` must still be deployed for Google signup to actually work. |
| 2026-09-19 | **UX5 (refined)** | **Keep the club count on the homepage; remove the roles and events counts.** Not the full removal originally logged — "725 clubs" is a true, durable credibility signal, whereas the other two will read **0 · 0** from launch day until clubs start posting. | Supersedes the original `UX5` wording. Also removes the read that downloads all ~725 club rows just to take `.length` from the LCP page — it now shares the cached directory query with `/clubs` instead. |
| 2026-09-19 | **O6 — error copy** | **A failed load must be visibly distinct from an empty result**, with a retry: *"We couldn't load the clubs just now."* + a **Try again** button. Genuinely-empty keeps *"No clubs yet — check back soon."* | Fixes `UX17c`, where a network failure was byte-identical to an empty database — the app confidently told visitors the site was empty when it wasn't. Satisfies Foundation rule 1 (never a dead end): the error state now offers the next action. Applies to all six migrated pages. |
| 2026-09-19 | **O12 — ClubDetail** | **Keep today's behaviour: wait for all four reads, then render the page at once.** Do NOT switch to progressive fill. | No visible change to a shipped page, and no layout jitter as pieces land. Caching means the wait only happens on a cold first visit; a return within `gcTime` is instant with no skeleton at all. |
| 2026-09-19 | **O13 — dead links** | **Render the not-found screen instead of redirecting to the list.** | The designed EmptyState at `OpportunityDetail.tsx:210-227` exists but is unreachable behind the `navigate("/opportunities")` at `:121`. Silently teleporting someone is the dead end Foundation rule 1 forbids — they assume they misclicked. The screen names what happened and offers "Browse all open roles". Also required regardless: a `queryFn` must not navigate. |
| 2026-09-19 | **UX15 wave 0** | **Foundation landed: one shared query-key registry (`src/lib/queryKeys.ts`, deliberately dependency-free) + `queryClient.clear()` on sign-out.** Key shape is `[table, discriminator, ...ids]` — **root is the table the rows come from, never the page that renders them**, so a table-shaped write invalidates every cached read of it wherever displayed. | Derived from a 9-file survey + reconciliation (10 agents, 0 errors). Pages are migrated in waves against this contract; the contract resolved 12 conflicts between independently-proposed keys and ranked 15 traps. |
| 2026-09-19 | **S6 outcome** | **No action — the cron token is the anon key.** Confirmed in production without ever printing the token. | The single largest unknown in the security review resolves to nothing. `grant_readonly_role.sql` is therefore **not needed** and should stay unused: standing read access to live student data was a poor trade for one question, and that question is now answered. |
| 2026-09-19 | **Deployed** | **Migration applied and `provision-oauth-user` deployed to production**, in that order. Vercel has the frontend. | `A1` (Google signup), `MB4` (`/help`), `MB7`/`UX4`, `D3`, `UX20`, `UX7`, `UX14` and the `S7`/`S8`/`S9` fixes are all now live. |
| 2026-09-20 | **Club "show applicant count" switch** | **Honour it everywhere, and do NOT change its default.** The switch works on a role's own page but is ignored on the roles list, so a club that turned it off still sees the number on the cards — a real inconsistency found while designing the counts fix. It now applies to both. Rejected removing the switch (takes away a choice clubs already have; a club with 2 applicants may not want that beside a club with 40) and rejected flipping the default to ON (it would silently overturn a setting clubs may already have chosen — note `EditOpportunity` currently defaults a null to **false**, so some counts will disappear from cards that show them today, which is the clubs' stated preference being respected). | One consequence to expect on launch: fewer role cards will carry a number than do now. |
| 2026-09-20 | **How hidden is "hidden"** | **A display preference, not a security boundary.** The count lives in a publicly readable column and the app hides it when the club asks; someone technical could still read it through the API. Accepted because it is an aggregate about a club's own posting — not personal data, and no student is identifiable from it. Rejected database-enforced hiding: it would cost an extra round trip on every list page, partly undoing the speed work just finished, to protect a number that is not sensitive. | Recorded explicitly so nobody later assumes the switch is a privacy guarantee. If that assumption ever needs to hold, the fix is a `SECURITY DEFINER` reader, not a policy tweak. |
| 2026-09-20 | **`O5-counts` semantics** | **Three answers that define what the numbers mean.** (1) **Events count CONFIRMED RSVPs only** — today the card counts everyone including people who cancelled, while the "spots left" figure beside it counts only confirmed, so the two numbers on the same card disagree. Confirmed-only makes them agree and matches the capacity trigger. Rejected counting pending-approval (can read higher than capacity) and showing both (a second number on a busy card, meaningful only for the minority of events that require approval). (2) **Roles count EVERY application ever submitted**, including ones the club has rejected — it is the honest measure of competition, which is what a student is judging, and it never goes down. Rejected excluding rejected ones: the number would visibly drop as a club reviews, which looks like applicants withdrawing and leaks how fast a club rejects. (3) **Hide the number entirely at zero.** A brand-new posting should look new, not ignored — and at launch nearly every card is at zero, so "0 applied" would be the least encouraging first impression the site could give. Rejected "Be the first to apply" as more marketing-toned than the app's voice. | Defines the migration: a confirmed-only counter on `events`, an all-applications counter on `opportunities`, and a frontend that renders the count only when it is above zero. |
| 2026-09-20 | **Push wave 4** | **Approved — pushed to `main`.** Frontend-only, so nothing to sequence ahead of it. | Closes `UX21` on the live site: the blank-application and blank-RSVP bug is no longer reachable. |
| 2026-09-21 | **`UX8` naming table** | **Signed-in addresses drop the role prefix**, and the club's own section is `/my-club`. Rejected keeping `/student/` and `/club/` (redundant for everyone who holds one account, which is everyone) and rejected a split scheme (an inconsistency with only two roles to justify it). Rejected `/club` for My Club: one character from `/clubs`, the public directory, so a typo lands somewhere entirely different and the two are confusable in speech and in support email. **The table, old → new:** `/student/dashboard`→`/activity`, `/student/profile`→`/profile`, `/student/profile/edit`→`/profile/edit`, `/student/messages` and `/club/messages`→`/messages` (one address, resolved by the account you are signed into), `/club/dashboard/opportunities`→`/postings`, `/club/dashboard/events`→`/postings/events`, `/club/opportunities/new`→`/postings/new`, `/club/opportunities/:id/edit`→`/postings/:id/edit`, `/club/events/new`→`/postings/events/new`, `/club/events/:id/edit`→`/postings/events/:id/edit`, `/club/dashboard` and `/club/dashboard/applications`→`/applicants`, `/club/dashboard/rsvps`→`/applicants/events`, `/club/dashboard/overview`→`/my-club`, `/club/dashboard/team`→`/my-club/team`, `/club/dashboard/analytics`→`/my-club/analytics`, `/club/profile`→`/my-club/edit`. Public addresses (`/opportunities`, `/events`, `/clubs`, `/help`, `/privacy`) already matched their labels and do not change. | **Every old address keeps working**, as a redirect, so nothing anyone has open or has linked breaks — and the two existing feed redirects set the precedent. This unblocks `UX8`, which the backlog had explicitly frozen pending this table. |
| 2026-09-20 | **Club discovery (`UX19`)** | **Clubs get a 4th nav item, `Discover`, which is a MENU rather than a destination** — it opens automatically on hover and offers Opportunities, Events and Clubs. Revised after the first mockup: making the label itself navigate meant picking one of three pages that are equally the point, which buried the other two behind a page the club never asked for. It reuses the student-facing lists — browse only, never act. A club could not see the rest of campus at all; not a missing link, a missing destination. Rejected calling it "Campus" (a word used nowhere else in the app) and rejected burying it inside My Club (a club with no reason to open My Club would never learn it exists). | Costs no new pages: `/opportunities`, `/events` and `/clubs` already refuse club actions server-side; what was missing was a way in, and cards that do not offer controls a club cannot use. **Hover-to-open is gated on `(hover: hover) and (pointer: fine)`**: on a touch device `pointerenter` fires on the same tap that is also the click, so an ungated version opens and instantly closes. A phone gets tap-to-open; click and keyboard work everywhere, which a hover-only menu would break for anyone not using a mouse. |
| 2026-09-20 | **Club card actions** | **A club sees a neutral `View` button in place of Save + Apply/RSVP.** Shown as a mockup both ways first. Rejected removing the buttons entirely: cards ended up different heights, the row read as ragged, and a card with no action looks broken rather than deliberate. Rejected keeping Save for clubs — saved items only surface on the student Activity page, so it would save into a void. | Also drops the `Saved` filter chip for clubs (another dead end) and the bookmark control in list view, which rendered regardless of whether the caller could handle it. Applied on the two list pages **and** on a club's public page, where the roles and events lists had the same Apply/RSVP buttons. |
| 2026-09-20 | **Page titles** | **The two discovery pages are titled `Opportunities` and `Events`, matching their nav words exactly.** The Opportunities page still said "Discover" while the nav said "Opportunities" — visible in every mockup. Rejected "Open roles"/"Upcoming events" (warmer, but reintroduces the mismatch we are fixing) and rejected keeping "Discover" on both (two nav items leading to two pages with the same title is worse than today). | The Opportunities subtitle is now role-aware: a club is not told to "find one, apply, show up" when it cannot apply. |
| 2026-09-20 | **`O5-counts` shipped** | **Migration applied to production, then the frontend pushed** — in that order, as the deploy rule requires. The frontend commit was deliberately held back while the database lacked the columns; pushing it earlier would have put the roles and events pages into their error state. | Verified live and read-only afterwards rather than assumed. Note for launch: 4 of the 5 seeded test roles have `show_application_count = false`, so their cards now carry no number — the switch being honoured, as decided. |
| 2026-09-21 | **Dead code (`DP12`)** | **Delete `DashboardLayout.tsx` and `Index.tsx`.** Nothing imports either; users see no change. Rejected leaving them logged: the file looks like real, current club-dashboard code while carrying five links to the `?tab=` scheme `ClubHome` abandoned, so the risk is a future session copying a pattern that was already fixed. | Confirmed unreferenced by a whole-tree grep, then by a clean typecheck and production build after removal — not by the grep alone. |
| 2026-09-21 | **Student profile photos** | **Skipped for launch — students keep the initials avatar.** Rejected the "unlisted" public bucket (fast and standard, but a photo viewable by anyone holding the link would be more exposed than the name beside it, which is signed-in-only) and rejected the strictly-private bucket (consistent, but signed URLs expire, slow the page and add moving parts). | No new privacy surface before launch, and nothing half-built: there is no avatar control in the edit form today, so no feature is advertised and missing. Raises the bar on the initials avatar, which is now the permanent launch treatment for every student and for the 136 clubs with no logo. The bundled migration therefore covers the LOGO storage only. |
| 2026-09-21 | **Push approval** | **The agent pushes frontend/app changes without asking, once tested. It still asks before a database migration, any data deletion, anything needing a maintainer-run command or secret, and any edge-function deploy.** Rejected auto-pushing docs and scripts only (would still have interrupted most rounds) and rejected the status quo. | Five pushes were approved in a row on 2026-09-21 with no change requested, so the question had stopped carrying information. `CLAUDE.md` § Non-negotiables was amended the same day to match — the agent still never writes to production itself, and "ask anyway if it is risky rather than merely visible" is written into the rule. |
| 2026-09-21 | **Logo resolution** | **Re-host the 320px variant, not the 100px one the manifest points at.** ZotSpot serves the same logo at several sizes from one path (`s1`=50, `s2`=100, `s3`=320, `s4`=640, no prefix=1800). The manifest carries `s2`. Rejected 100px — the club detail hero renders at **88 CSS px**, so a 100px source is visibly soft on any 2× screen, on the one page the club itself will look at. Rejected 640px and the 1800px original as storage spent on pixels nothing displays. | ~55–80 KB each. A real dry run over 25 clubs came to 2 MB, so **roughly 45 MB for all 589** — against a 1 GB allowance and a 10 MB per-file bucket limit. 40 of 40 sampled clubs have the 320px variant; the script still falls back to the manifest URL per club and reports every fallback, because "40 of 40" is not 589 of 589. |
| 2026-09-21 | **Logo storage — no migration needed** | **Use the existing `club-assets` bucket; this is a script, not a migration.** The doubted blocker is resolved: the bucket is public, already carries the `S9` mime and size limits, and the service role bypasses RLS, so no new bucket and no policy change. Files land at `seeded/<club_id>/logo.<ext>`, which cannot collide with a claiming club's own `<uid>/…` uploads. | Removes a maintainer-run migration from the plan entirely — one script, one command. The `--commit` path still opens with a canary upload-and-delete so the RLS assumption fails loudly and early rather than 300 clubs in. |
| 2026-09-21 | **Logo re-host — who runs it** | **The agent writes and tests it; the maintainer runs one command.** Same shape as the `O5-counts` and `S7`/`S9` migrations. The script dry-runs by default and prints exactly what it would do; the maintainer supplies the service-role key and performs the only irreversible step. Rejected running it in 50-club batches (more maintainer time for a risk the dry run and the idempotent key already cover). | Keeps the non-negotiable intact — the agent never writes to production — while leaving the maintainer a single reviewable action rather than a supervision job. |
| 2026-09-21 | **Logo manifest** | **The maintainer will locate and upload the original ZotSpot import manifest; the agent verifies it is the right file and sufficient before anything is built on it.** Not in the repo, and `source_logo_url` is not readable by the anon key (column-level grant), so the 589 URLs cannot be audited from the app side at all. | `scripts/verify_logo_manifest.mjs` exists for exactly this and is read-only. If the file turns out to be the wrong vintage, the mismatch surfaces before a bulk write rather than halfway through one — the seeder keys on `source_club_id` and would treat unknown ids as new clubs. |
| 2026-09-21 | **Follow notifications (`UX24`)** | **Fix it in the frontend rather than with a migration.** The notification works out which page to open from its own generated title (`New opportunity from …` / `New event from …`). Rejected the migration that would split the type into `new_opportunity` / `new_event`: cleaner forever, but it costs a maintainer-run command and leaves every already-sent notification on the old broken value. Rejected shipping the empty state without mentioning following. | Named cost: the fix depends on a string OUR OWN trigger generates, so changing that title breaks the link silently. A comment pins the dependency at both ends. Fixed before the empty-state copy ships, because that copy promises following. |
| 2026-09-21 | **Empty-state copy (`UX17a`/`UX17b`)** | **"Nothing open right now — but 722 clubs are here", with the number pulled LIVE and the button reading "Browse all 722 clubs".** Events gets the mirrored version. Rejected leading with the number alone (a visitor might read an empty roles list as still loading) and rejected the quieter button. | Both empty lists now point at Clubs — the only surface with content on launch day (722 clubs, 0 roles after `D1`, 0 events). That breaks the loop `UX17a` logged, where each empty page promised the other empty one. The count is live, so it satisfies "live counts only" and can never go stale. |
| 2026-09-21 | **Role-less accounts (`A5`)** | **A signed-in account with no role goes to a new `/account-setup` screen** — "We're still finishing your setup", with a retry, a Help link and a way to keep browsing. Rejected sending them to the waitlist page (they have no waitlist entry, so "we're reviewing your application" would have them waiting for a review that never comes) and rejected signing them out (alarming, and it loops if the cause persists). | This state means provisioning did not finish, so the copy says that rather than inventing a queue. The page is deliberately a PUBLIC route: guarding it would bounce exactly the people it exists for. |
| 2026-09-21 | **When entitlement cannot be read (`A5`)** | **Neither let them in nor lock them out — show a retry.** A failed role or waitlist read now renders "We couldn't check your account" **in place**, keeping the URL so a successful retry returns to the page they wanted. Rejected failing closed (the textbook answer, but one flaky request would eject an approved student with no explanation) and rejected today's fail-open. | The same rule already applied to the club and event lists by the `O6` decision — a failed load must be visibly distinct from an empty result. This extends it from "no data" to "no answer", on a guard where the stakes are higher. |
| 2026-09-21 | **Home-page hero CTAs (`UX12`)** | **"Start exploring" now goes to Opportunities for EVERYONE**, signed in or out. Rejected making it auth-aware (signed out → signup) — the maintainer chose the simpler rule that can never be wrong. **"Browse clubs" keeps its label and gets the right destination, `/clubs`** — rejected renaming it to "Browse roles", which would have left the home page with no prominent way into the 725-club directory. | The hero no longer pushes signup at all; that path is carried by the top bar's **Get started** and by the two split cards. Side effect worth knowing: Google sign-in always returns to the home page, so this is also the first button a newly signed-in user sees — it now leads somewhere real. |
| 2026-09-21 | **Home-page split-card CTAs (`UX9`, `UX10`)** | **Signed out → signup; signed in → the real page.** "Explore clubs" → `/clubs`; "Bring your club to ZotHub" → `/my-club` for a club account and `/clubs` for a student (a student cannot claim a club — claiming is a signed-out flow — so a signup form would be a dead end for them). Rejected always skipping signup (loses two of the home page's entry points) and rejected leaving both pointing at signup (a signed-in visitor is silently bounced back where they started, which reads as a broken button). | These two buttons are what `UX9`/`UX10` logged. The fault was invisible in review: the `to` was a real route and the page rendered — the failure was one hop later, in `/signup`'s redirect. |
| 2026-09-21 | **"Apply"/"RSVP" for logged-out visitors** | **Left as is.** A visitor sees the real verb, clicks, and the detail page then offers "Log in to apply". Rejected showing "View" until signed in: strictly more honest, but a page of "View" tells a first-time visitor nothing about what the site is for. | Recorded so it is not "fixed" later by someone reading it as an oversight. The two-step is deliberate. |
| 2026-09-21 | **Toolbar shape (`UX11`)** | **The filters that are about the VIEWER stay visible; the taxonomy filters move into one `Filter` menu.** Saved and the clubs you follow remain chips because they are the reason a student comes back; role type, club category and the date window go behind the menu. Chosen from three mockups. Rejected keeping every filter as a chip (tallest on a phone — three stacked rows before the first card, with half the types hidden in a horizontal overflow nobody scrolls) and rejected the backlog's original "everything in the menu" (tidiest line, but Saved disappears and stops being discovered by accident). | Types became **multi-select** in the move, which the single-select chip row could not express at all: a student wanting Leadership *or* Creative had to look twice. Saved and Following became independent toggles that AND together, so "saved roles from clubs I follow" is now expressible. The `All` chip is gone — the way out of a filter is the same control you came in by. |
| 2026-09-21 | **Events sort (`UX13`, contract `O3`)** | **Events gains `Soonest first` / `Most popular`, applied by the DATABASE with the choice in the query key.** Rejected sorting in the browser (instant, and correct only while there are fewer than 50 upcoming events — after that "most popular" silently means "most popular among the next 50 by date"). Rejected leaving Events with no sort. | The named cost: switching sort is now a network round trip, not an instant reshuffle. `placeholderData: keepPreviousData` keeps the current rows on screen, dimmed, so it does not flash back to skeletons. |
| 2026-09-21 | **Opportunities sort accuracy** | **Fix it now rather than log it.** `Closing soonest` and `Most applied to` were re-ordering the 50 NEWEST roles in the browser, so past 50 open roles they would have answered the wrong question while looking perfectly correct. Both now order server-side before the cap, on the same pattern as Events. | Nothing changes visibly today — production currently has 5 active roles. This was a time bomb set for the first busy term, and the same class of fault `O3` had already flagged for Events. |
| 2026-09-21 | **Clubs list view (`UX13`)** | **Clubs gets the same card/list toggle as the other two pages.** ~725 clubs is the longest list in the app and was the only one with cards-only; the dense list fits roughly ten names per screen against three. | Deliberately a new `ClubList`, not `DiscoverList`: that component groups rows BY CLUB and gives each a primary verb, so a club would have appeared as a header above a single row of itself. |
| 2026-09-21 | **Clubs count line** | **Only shown once something is filtered.** The Clubs header already states the unfiltered total, and printing "725 clubs" twice on one screen reads as a mistake. Opportunities and Events have no count in their header, so theirs always shows. | A deliberate asymmetry between the pages, not drift — recorded here so it is not "corrected" later. |
| 2026-09-20 | **`O5-counts` delivery** | **Same shape as the `S7`/`S8`/`S9` migration:** one migration file, proven against a throwaway local database first, then a single command for the maintainer to run. Not raw SQL pasted into the dashboard. | Keeps the schema history in the repo — pasting into the dashboard is how the migration history got out of sync before. |
| 2026-09-19 | **Work order** | **Wave 4 before the `O5-counts` trust fix.** Finish the caching workstream in one unbroken run rather than interrupting it, because wave 4 is where `UX21` lives — a student who browses logged out and then signs in can currently submit an application or RSVP with **blank answers**, which outranks the wrong card numbers even though it is far less visible. Rejected doing both at once: a database migration deserves its own review, not one shared with three page rewrites. | `O5-counts` becomes its own step immediately after wave 4, ahead of the nav restructure. |
| 2026-09-19 | **Push wave 3** | **Approved — push to `main` and let Vercel ship it.** Frontend-only: no migration, no edge function, so the deploy-order rule has nothing to sequence. | Also ships two live-site fixes found during the wave: the signed-out "Saved" dead end (`UX22`) and `A4`, where signing in did not clear the cache. |
| 2026-09-19 | **O5-counts** | **Fix the wrong applicant/attendee counts BEFORE launch.** A visitor sees "0 applied" on every role even when twelve people have applied, because RLS hides other people's rows from the reader. Framed and accepted as a **trust** problem, not a performance one: showing 0 everywhere makes a busy campus look dead to exactly the audience that has not signed up yet. Rejected hiding the counts (loses the social-proof signal) and deferring (the 0 is the default first impression). | Needs a viewer-independent count — a `SECURITY DEFINER` aggregate or a denormalised counter — so it is a **backend migration** and cannot ride along in a frontend wave. Scheduled as its own step; see `O5-counts` in the audit table. |
| 2026-09-19 | **UX23 — Apply button** | **Grey the Apply button out until we know whether the student has already applied.** Rejected leaving it (a fast tap still produces a raw "You have already applied") and rewording the error instead (fixes the symptom, leaves a button that lies). | The word and the button size do not change, so nothing shifts when the answer lands — only the affordance goes. Applies to both the card and the list row. Verified in a browser both ways: with the fix the control is a disabled `<button>`; with it removed the same control is a live `<a>` a student can tap. |
| 2026-09-19 | **UX15 freshness (T13)** | **Keep the 60-second freshness window on the Events and Roles lists** — the same one the Clubs page already uses. Rejected 15s (4x the database reads, and the skeleton would keep reappearing) and 5 minutes (a club posting a role would not see it on the public list for up to 5 minutes, which reads as broken to *them*). | Accepted, named consequence: an event that has just started, or a role whose deadline has just passed, can stay listed for up to ~60s of active use (up to `gcTime` = 5 min across a remount). Nobody can *act* on a stale row — the detail page re-checks `deadlinePassed` and the DB trigger is authoritative on capacity — so the drift is cosmetic. This follows from caching, not from a bug. |
| 2026-09-19 | **UX22 — signed-out "Saved"** | **Fix the dead end now: a logged-out visitor who taps the Saved chip gets a sign-in prompt, not "you haven't saved any events yet".** Rejected hiding the chip (the toolbar would change shape depending on auth state, and the nudge to sign up is worth keeping). | Turns a dead end into a signup path, and satisfies Foundation rule 1 (never name a problem without offering the remedy). Shipped inside wave 3 on both Events and Opportunities. |
| 2026-09-19 | **O4 / T4 — the 50-row cap** | **Log it, fix after launch.** The two list pages load only the newest 50 rows, so "Saved" / "Following" / "This month" only search within those 50. Rejected raising the cap to 200 (papers over it) and fixing it properly now (paging or server-side filtering is a redesign of how both pages load, and would make the caching diff unreviewable). | Unreachable in practice until a page carries 50+ live postings; there are ~5 today. Caching makes the arbitrary window *persist* rather than be re-rolled per visit, so it will feel more settled than it is — that is the thing to watch, and it is why this is written down rather than left implicit. Tracked as `T4`/`MB3`. |
| 2026-08-23 | **Prod access** | **Mode B: read-only database access for the agent; every write runs through the maintainer.** The agent may verify live state itself; it may not change a row, upload a file, or delete anything. Writes are prepared as a tested script/SQL the maintainer runs. | Removes most verification round-trips without putting a write credential in the agent's hands. `CLAUDE.md`'s "never write to production" stands unchanged. |
| 2026-08-23 | **P1-license** | **CLOSED — the maintainer took the hero photo and owns it.** | `public/images/hero-campus.jpg` needs no replacement. Record the ownership in the repo so this is never re-litigated. `P1` (WebP/AVIF conversion) is unaffected and still open. |
| 2026-08-23 | **MB4** | **One solid `/help` page**, no support backend: FAQ sections (students / clubs / accounts & privacy), a Contact block with `zothub.uci@gmail.com`, and a "Report a problem" link that opens a pre-filled email. An in-app ticket form was considered and rejected as more than a campus beta needs. | Closes every "go through Help/Contact" promise the app already makes (claim emails, the no-self-service-removal policy, and — now that `MB6` is post-launch — the privacy policy's manual-deletion route). No new table, no new email path, no spam surface. |
| 2026-08-23 | **UX8** | **Rename the routes** so addresses match their labels, with redirects from the old paths. Now is the cheapest moment — nothing is bookmarked or linked yet. **The specific names are a separate decision, being taken with the maintainer.** | ✅ Table filled in 2026-09-21 — see the row above. |
| 2026-08-23 | **MB6** | **Account deletion is POST-LAUNCH.** Not a launch blocker. | `MB6` moves out of Launch blockers. `/privacy` must therefore keep stating that deletion is a manual request to the support address — and `MB7`/`UX4` (making that address reachable) becomes *more* important, not less. |

---

## Decisions needed (no work until answered)

| # | Question |
|---|---|
| 1 | **N8** — should RSVP confirmation emails respect notification preferences? The PRD contradicts itself. |
| 2 | **MB9** — RSVP confirmation-email consistency, tagged `[launch-blocking, small]` in `prd.md` but never specified. Same area as N8; resolve together. |
| ~~3~~ | ~~**MB6** — how far does account deletion go?~~ **ANSWERED 2026-08-23: moot for launch — deletion is post-launch.** Revisit when it is built. |
| 4 | `/admin` review cadence during club outreach (from the superseded 10-day plan, never settled). |
| 5 | **MB2** — storage bucket policy for student avatars (Bucket B decision was deferred). |

---

## Done

**Shipped to production**
- **MB5 seed** — 724 ZotSpot clubs seeded, published, with unclaimed-profile treatment.
- **MB5 claim flow** — logged-out-only admin-reviewed claiming; edge functions
  `submit-club-claim` / `review-club-claim`; `/admin` claims panel; migrations
  `20260727000200`/`00300`/`00400`/`00500`; Turnstile live both sides. Deployed 2026-07-27.
  Design: `docs/design/mb5-claim-flow.md`. Tests: `tests/e2e` (**115/115**).
- **Signup/email hardening** — DB `@uci.edu` gate kept authoritative with one-time
  authorizations; `email_verifications` RLS locked down; `send-email` strict allowlist +
  service-role / authoritative-derive tiers + HTML escaping; one shared delivery check
  (HTTP 200 + `{error}` = failure); atomic email-keyed rate limits; pending clubs
  `published=false` until admin approval.
- **S1** `user_roles` self-insert privilege escalation — migration applied to prod.
- **S3** Day-0 auth, students auto-approved — `verify-otp` deployed (v3 ACTIVE).
- **DP1** favicon / og:image / apple-touch / manifest / theme-color.
- **DP2** outlined-glyph brand SVGs + in-app wordmark aligned to spec.
- **N9** prod apply/deploy state — resolved.

**Verified closed — do not re-plan**
- Broken routes (`/reset-password`, `/student/messages?to=`, NotificationCard target).
- `normalizeOpportunityType()` coercion removed; Clubs category filter data-derived;
  `reviewed` status settable; chart colours tokenised.
- Accessibility sweep — labelled controls, 44px targets, AA contrast. No open a11y items.
- Route-level code splitting; recharts out of the eager bundle.

---

## Where the old docs went

| Doc | Status |
|---|---|
| `docs/LAUNCH-BACKLOG.md` | **Replaced by this file.** |
| `docs/archive/00-handoff.md`, `docs/archive/implementation-audit.md` | Archived — were self-marked superseded. Open items extracted here. |
| `docs/archive/notes-superseded-10-day-plan.md` | Archived. **Still holds the Test Club purge runbook for D1** (`:100-108`). |
| `plan.md` | Kept as engineering history. WS10–12 and the Lovable checklist are tracked here (`H2`, `MB4`, `MB8`). |
| `prd.md` | Kept — product definition, not a tracker. Its untracked `[launch-blocking]` tags are now `MB6`–`MB9`. |
| `docs/design/*`, `docs/strategy/*` | Kept as reference. Their "Open / Still open" sections point here. |
| `docs/design/mb5-claim-flow.md` | Kept — as-built design record. §11 rollout is **done**. |
